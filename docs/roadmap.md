# Roadmap atualizado

Uma única página web, com todas as contas. Cada linha é uma fatia utilizável sozinha; a primeira
prova o caminho de ponta a ponta (tela React → API FastAPI → SQLite) e as outras crescem sobre ela.

| # | Fatia | O usuário consegue… | Origem | Depende de | Status |
|---|---|---|---|---|---|
| 001 | `login-e-saldos` | entrar no painel e ver o saldo de hoje de cada conta | Pedido de 22/09 | — | done |
| 002 | `atualizar-registros` | apertar um botão e ter todos os registros bancários atualizados na base, vendo quando foi a última atualização | Pedido de 22/09 | 001 | done |
| 003 | `lista-de-gastos` | ver uma lista paginada com todos os gastos de todas as contas | Pedido de 22/09 | 001 | done |
| 004 | `ordenar-gastos` | ordenar os gastos por data, valor ou categoria | Pedido de 22/09 | 003 | done |
| 005 | `filtrar-por-periodo` | filtrar os gastos por período (mês, intervalo de datas) | Pedido de 22/09 | 003 | done |
| 006 | `filtrar-por-conta` | filtrar os gastos por banco ou conta | Pedido de 22/09 | 003 | done |
| 007 | `buscar-por-texto` | encontrar gastos pela descrição ou pelo nome de quem recebeu | Pedido de 22/09 | 003 | done |
| 008 | `total-por-categoria` | ver os gastos do período agrupados por categoria, com o total de cada uma | Pedido de 22/09 | 005 | done |
| 009 | `ajustar-categoria` | trocar a categoria de um gasto, e a troca sobreviver à próxima atualização | Pedido de 22/09 | 003 | done |
| 010 | `criar-categoria` | criar, renomear e apagar categorias | Pedido de 22/09 | 009 | done |
| 011 | `categoria-para-parecidos` | aplicar a mesma categoria a todos os gastos parecidos de uma vez | Pedido de 22/09 | 009 | done |
| 012 | `limite-por-categoria` | definir um limite mensal para cada categoria | Pedido de 22/09 | 010 | done |
| 013 | `sinal-por-categoria` | ver, em cada categoria, se está dentro, acima ou abaixo do limite no período | Pedido de 22/09 | 008, 012 | done |
| 014 | `sinal-do-mes` | ver se o total do mês está dentro, acima ou abaixo do teto do plano de recuperação | Pedido de 22/09 | 005 | done |
| 015 | `marcar-nao-gasto` | tirar dos totais um lançamento que não é gasto (transferência entre contas próprias, estorno) | Pedido de 22/09 | 003 | done |
| 016 | `entradas` | ver as entradas (salário e outras receitas) separadas dos gastos, no mesmo período | Pedido de 22/09 | 005 | done |

## Dívidas técnicas

| # | Fatia | Resolução necessária | Origem | Depende de | Status |
|---|---|---|---|---|---|
| 017 | `jinja-router-extraction` | manter a lógica de consulta única quando uma tela migra de Jinja para React, em vez de reescrever SQL | bug | 001 | done |
| 018 | `sync-runs-source-field` | rastrear origem do disparo (tela vs comando diário) em `sync_runs.source` em vez de só guardar o caminho do arquivo lido | 002 | — | done |
| 019 | `pluggy-extract-env-handling` | embrulhar leitura de `.env` e `SystemExit` em `ingestao/pluggy_extract.py` em função limpa, não chamar direto do serviço | 002 | — | done |
| 020 | `jinja-sync-button-exclusive-lock` | fazer o botão Jinja `/sincronizar` passar pela trava de execução única de `app/sync/exclusive.py` | 002 | — | done |
| 021 | `pluggy-connections-editable-ui` | mover lista de conexões Pluggy de `data/item_ids.txt` para tela editável (norma 26) | 002 | — | done |
| 022 | `setup-secrets-pluggy-credentials` | atualizar `docs/setup-secrets.md` para citar `PLUGGY_CLIENT_ID` e `PLUGGY_CLIENT_SECRET` | 002 | — | done |
| 023 | `category-labels-in-schema` | mover rótulo em pt-BR das categorias de `app/taxonomy/seed.json` para coluna `label` na tabela `categories`, de modo que SQL possa ordenar e filtrar por categoria em 004 e 006 | 003 | — | done |
| 024 | `payee-filling-at-ingest` | preencher `payee` na própria `ingest`, na transação da carga, para que toda base (sincronização, testes, e2e) nasça com recebedor; a classificação segue no pós-carga | 003 | — | done |
| 025 | `spending-filter-predicate-unification` | unificar predicado de data duplicado em `app/queries/spending.py` (`total_spending_cents`) e `app/queries/expenses.py` (`_where`) em `spending.py`, evitando divergência na soma por categoria (008) | 005 | — | done |
| 026 | `unify-payee-name-precedence` | consolidar precedência do nome do recebedor entre Python (`app/payees/names.py`, `_chosen`) e SQL (`app/queries/expenses.py`, `_PAYEE_NAME_SQL`), evitando divergência silenciosa se uma mudar | 007 | 025 | done |
| 027 | `expenses-list-mock-unification` | `filterForSpy` em `src/features/expenses/components/__tests__/expenses-list.test.tsx` duplica a lógica de filtro dos mocks; importar `filterExpenses` de `src/testing/mocks/handlers.ts` em vez de reinventar | 008 | — | done |
| 028 | `manual-category-classify-precedence` | o ajuste manual de categoria (`transactions.category_source = 'manual'`) não tem precedência sobre regras por descrição no agrupamento (`group_id`) usado pelas telas Jinja antigas (`app/taxonomy/classify.py`, `_match`); enquanto essas telas existirem, um gasto ajustado à mão pode aparecer em outro grupo nelas | 009 | — | done |
| 029 | `jinja-labels-from-schema` | `app/routers/spending.py` e `app/routers/rules.py` montam `LABELS` do seed no import e não veem rótulo renomeado nem categoria criada pelo dono; ler `categories` em requisição nas duas telas Jinja (e em `tests/test_gastos_screen.py`) | 010 | 010 | done |
| 030 | `shared-categories-query` | `src/features/expenses/api/get-categories.ts` e `src/features/categories/api/get-categories.ts` buscam `GET /api/categories` com a mesma chave `['categories']`; mover hook e tipo para `src/hooks/` e `src/types/` pela regra "usado por duas features → compartilhado" | 010 | 010 | done |
| 031 | `expense-count-label` | `pagination.tsx:19`, `category-totals.tsx:57` e `similar-offer.tsx` escrevem o plural de "gasto" cada um do seu jeito; um utilitário em `src/features/expenses/utils/` usado pelos três | 011 | — | done |
| 032 | `e2e-seed-classifies` | o recebedor já nasce preenchido na carga, mas o seed de `scripts/e2e-backend.sh` não roda `classify_all`, então grupo, natureza e essencialidade ficam sem classificação automática; aplicar a classificação no seed | 011 | 024 | done |
| 033 | `income-predicate-unification` | unificar o predicado de "entrada" (receita) que está escrito à mão em `app/projection/forecast.py`, `app/projection/monthly.py` e `app/plan/objective.py` em `app/queries/spending.py` como `INCOME`, antes de 016, para evitar divergência entre plano e tela de entradas | 015 | — | done |
| 034 | `override-unnecessary-reclassify` | `app/taxonomy/override.py::_write` reclassifica tudo via `classify_all` mesmo quando a coluna escrita (`not_expense_reason`) não é lida pela classificação; remover custo desnecessário a cada marcação de não-gasto | 015 | — | done |
| 035 | `mocks-spending-rule-duplication` | os mocks MSW em `src/testing/mocks/handlers.ts` (`matchesView`, `period-result`) duplicam a regra de entrada/gasto da API; se `app/queries/spending.py` mudar, os testes de componente continuam verdes com regra antiga | 016 | — | done |
| 036 | `e2e-expenses-reload-flaky` | `e2e/expenses.spec.ts` ("filters the expenses by month and by date range and keeps the period on reload") falhou uma vez em 25/09 com 2 itens em vez de 1 depois do `page.reload()`, com a URL já em `from`/`to` do dia 02; passou na repetição. Achar por que a lista recarregada ignora o intervalo às vezes antes que o flake trave um PR | 017 | — | done |
| 037 | `e2e-suite-repeatable` | rodar `pnpm test:e2e --repeat-each=N` na suíte inteira para caçar teste instável; hoje a segunda volta de `e2e/sync.spec.ts` ("updates the records from the balances page") falha porque espera "Nunca atualizado" numa base que a primeira volta já sincronizou | 036 | — | done |
| 038 | `e2e-ceiling-reload-flaky` | `e2e/expenses.spec.ts` ("shows the month against its ceiling, edits the ceiling inline and leaves the base as it found it") falhou 1 vez em 40 execuções (duas rodadas de `--repeat-each=20`) em 25/09: depois do `page.reload()` a região "Teto do mês" não apareceu em 5 s. Como o teste só devolve o teto ao estado inicial no fim, a falha deixa o teto gravado e derruba todas as repetições seguintes; achar a causa e fazer a limpeza não depender do teste passar | 036 | — | done |
| 039 | `extract-script-reads-connections` | `ingestao/pluggy_extract.py` (`criar-item`, `status`, `extrair`) ainda lê e grava as conexões em `data/item_ids.txt`, que a atualização do painel não lê mais desde a 021; ler e gravar em `pluggy_connections` para o script manual e a tela não divergirem | 021 | 021 | done |

## Revisão geral de 25/09/2026

Achados da revisão do back, do front e da experiência de uso, em ordem de precedência: primeiro o
atalho de execução pedido pelo dono, depois número de dinheiro errado, perda de dado ou usuário
travado, quebra, e por fim usabilidade.

| # | Fatia | Resolução necessária | Origem | Depende de | Status |
|---|---|---|---|---|---|
| 040 | `vscode-single-run-config` | `.vscode/launch.json` passa a ter uma só configuração, "Executar", que prepara a base como a tarefa "Preparar a base local" (`.vscode/tasks.json`), sobe a API e o Vite e abre `http://localhost:5173/app/` no navegador padrão do sistema; as outras entradas e o `compounds` saem. O dono relata que o arranjo atual não abre o navegador: provar a causa antes de trocar. Suspeitas: o `stopAll: true` do compound derruba API e front quando "Abrir o painel no navegador" termina com `exit`, e o `xdg-open` disparado de dentro do terminal morre junto com ele | revisão geral 25/09/2026 | — | done |
| 041 | `consolidation-local-date` | `ingestao/pluggy_consolidate.py` (~198) corta a data do lançamento em UTC (`date[:10]`) em vez de `America/Sao_Paulo`: 261 lançamentos caem no dia seguinte, 13 mudam de mês e 4 salários do dia 31 às 23:59 entram no mês seguinte. Esta e as fatias 042 a 045 mudam números históricos, inclusive os de referência congelados em `docs/plano.md` (norma 28): cada uma reconcilia esse documento no mesmo PR, com o número antigo, o novo e a causa | revisão geral 25/09/2026 | — | done |
| 042 | `consolidation-latest-copy` | a consolidação guarda a cópia bruta **mais antiga** de cada lançamento (`ingestao/pluggy_consolidate.py` ~119 e ~179): 77 linhas desatualizadas, compras pendentes que sumiram da Pluggy (R$ 125,88) continuam somando e parcelas ficam com data errada; ficar com a cópia mais recente e descartar o pendente que a Pluggy não devolve mais | revisão geral 25/09/2026 | 041 | done |
| 043 | `installment-credit-not-income` | o crédito de parcelamento de fatura ("CREDITO PARCELAM. TODAS FATURAS" R$ 8.579,27 em set/2026; "Crédito de parcelamento" R$ 3.976,38 em out/2025) conta como entrada enquanto as parcelas contam como gasto, somando o mesmo dinheiro duas vezes; o mesmo evento é classificado diferente conforme a descrição; e o empréstimo consignado de R$ 32.000 entra como receita no resultado do período (`ingestao/pluggy_consolidate.py` ~80-83). Uma regra só para crédito de parcelamento e empréstimo, fora de entrada e de gasto | revisão geral 25/09/2026 | 042 | in-progress |
| 044 | `transfer-pairing-strict` | o pareamento de transferência entre contas próprias (`ingestao/pluggy_consolidate.py` ~301-325) casa qualquer débito e crédito de mesmo valor em contas diferentes num intervalo de 3 dias, sem olhar a descrição, e tira do total gasto real pareado com Pix recebido de terceiro (norma 25 no sentido inverso); exigir sinal de transferência na descrição e o par mais próximo | revisão geral 25/09/2026 | 043 | planned |
| 045 | `refund-same-account` | o casamento de estorno (`ingestao/pluggy_consolidate.py` ~364-376) aceita débito de outra conta: o estorno da anuidade anulou um pagamento de conta; estorno só casa com débito da mesma conta | revisão geral 25/09/2026 | 044 | planned |
| 046 | `money-input-keeps-saved-value` | digitar "1.500,00" no teto do mês (`src/features/expenses/components/ceiling-form.tsx:67`) ou no limite da categoria (`src/features/categories/components/category-limit-form.tsx:68`) apaga o valor gravado sem aviso, porque o `input type="number"` devolve vazio e `src/utils/money-text-schema.ts` trata vazio como "sem valor"; campo de texto com formato brasileiro. No mesmo esquema e formulários: limite negativo mostra "Use no máximo duas casas decimais." em vez de "Informe um valor maior que zero.", salvar o teto vazio fecha sem dizer nada, e o botão continua "Definir teto" quando o teto já existe | revisão geral 25/09/2026 | — | planned |
| 047 | `expired-session-redirect-loop` | com a sessão expirada, `/login` e `/` se redirecionam sem fim (`src/app/routes/login.tsx:15` contra `src/lib/auth.tsx:39`) e o usuário não consegue entrar de novo | revisão geral 25/09/2026 | — | planned |
| 048 | `date-range-typing` | nos campos "De" e "Até" dos gastos, digitar a data pelo teclado apaga dia e mês a cada tecla e o filtro nunca se aplica (só o calendário funciona); "Até" antes de "De" descarta o "De" em silêncio, sem mensagem | revisão geral 25/09/2026 | — | planned |
| 049 | `not-expense-total-sign` | a visão "Não são gastos" soma saídas e entradas e depois tira o sinal (`src/features/expenses/components/pagination.tsx:26`, `formatMoney(Math.abs(totalCents))`, alimentado em `expenses-list.tsx:421`): transferência de −1.000 e +1.000 aparece como R$ 0,00; mostrar saídas e entradas separadas | revisão geral 25/09/2026 | — | planned |
| 050 | `api-integer-range-422` | inteiro fora da faixa do SQLite devolve 500 em página da lista, `PUT /api/plan/ceiling`, `PUT` do limite de categoria, id de lançamento e degrau de `/dividas/taxa`, e `/gastos?fim=9999-12-31` quebra em `app/queries/period.py:56-58`; validar faixa na entrada e responder 422 | revisão geral 25/09/2026 | — | planned |
| 051 | `spa-not-found-route` | a SPA não tem rota de 404 nem de erro (`src/app/router.tsx:12-48`): `/app` sem barra final fica em branco, caminho desconhecido mostra o erro do React Router em inglês, e a consulta do usuário sem nova tentativa em erro que não é 401 cai nessa mesma tela | revisão geral 25/09/2026 | 047 | planned |
| 052 | `mobile-no-horizontal-scroll` | em 375 px toda tela da SPA rola na horizontal (410 px de largura) e o link "Conexões" fica cortado no cabeçalho | revisão geral 25/09/2026 | — | planned |
| 053 | `sync-refreshes-all-views` | depois de "Atualizar agora" só os saldos são buscados de novo (`src/features/sync/api/run-sync.ts:14-17`); lista de gastos, seletor de contas e uso por categoria ficam com o dado de antes até recarregar a página | revisão geral 25/09/2026 | — | planned |
| 054 | `expenses-opens-current-month` | "Gastos" abre em "Todo o período", onde o resultado do período e o teto do mês não aparecem; abrir no mês corrente (muda o padrão decidido na 005) e pôr resultado e teto antes dos filtros | revisão geral 25/09/2026 | 048 | planned |
| 055 | `balances-month-plan-card` | a tela inicial de saldos não responde "estou dentro do plano este mês?"; um cartão com o mês, o teto e o resultado até hoje | revisão geral 25/09/2026 | 054 | planned |
| 056 | `spa-legacy-navigation` | o cabeçalho da SPA não leva às telas antigas (Resumo, Objetivo, Dívidas, Simulador, Consultor, Configuração) e o plano de saída do déficit só existe nelas; navegação única entre as duas até a migração terminar | revisão geral 25/09/2026 | 052 | planned |
| 057 | `single-money-formatter` | o sinal do dinheiro varia entre telas ("-R$" com hífen e "−R$", total positivo em vermelho, negativo diferente nas telas antigas); um formatador e uma regra de sinal para a SPA e o Jinja | revisão geral 25/09/2026 | 049 | planned |
| 058 | `category-search` | a página "Categorias" lista ~80 linhas sem busca nem prioridade; o seletor de troca de categoria é uma lista de 78 opções sem busca, `Esc` devolve o foco para o topo da página e a etiqueta não parece editável | revisão geral 25/09/2026 | — | planned |
| 059 | `accessible-names-and-targets` | nomes acessíveis não começam pelo texto visível ("Não é gasto" é lido como "Marcar X como não-gasto") e os links do cabeçalho têm 20 px de altura ("Sair", 15 px); nome acessível começando pelo rótulo e alvo de no mínimo 24 px | revisão geral 25/09/2026 | 056 | planned |
| 060 | `plain-language-copy` | textos que o dono não tem como usar: o Resumo antigo manda rodar `python -m app.sync`, a Configuração mostra caminho de arquivo como texto de link, "Sinal só por mês" e "Pluggy" aparecem sem explicação, e os saldos mostram "Pedida na tela", "Concluída" e "Nunca atualizado" enquanto as contas dizem "Atualizado em 05/09" | revisão geral 25/09/2026 | — | planned |
| 061 | `not-expense-reason-inline` | o seletor de motivo de "Não é gasto" espreme a descrição do lançamento na linha; abrir a confirmação abaixo da linha | revisão geral 25/09/2026 | 059 | planned |
| 062 | `e2e-seed-card-sign` | o cartão da base de teste tem saldo `-45.0` em `tests/data/e2e_accounts.json`, com o sinal invertido em relação ao que a Pluggy manda, e o Resumo antigo soma a dívida do cartão como dinheiro em caixa; corrigir o seed | revisão geral 25/09/2026 | — | planned |
