# PLAN 008 — total por categoria

Branch: `feature/008-total-por-categoria`

Decisões registradas aqui (a SPEC deixou ao plano; escolhido o mais simples):

- A chave `Health` do fixture e2e (`FARMACIA CENTRAL`, `e2e-t-5` em `tests/data/e2e_transactions.json`) **não existe** em `app/taxonomy/seed.json`; a chave do seed é `Healthcare`, com rótulo "Plano de saúde" (o grupo dela é "Saúde", mas o que a API devolve em `label` é o rótulo da categoria, como `list_expenses` já faz). O fixture passa a usar `Healthcare` em `categoria_pluggy` e `categoria` de `e2e-t-5`, e o e2e afirma "Plano de saúde" na segunda linha. Nenhum e2e existente afirma o rótulo dessa linha, então nada mais muda.
- Dois `role="status"` na mesma tela (lista e bloco) quebram `getByRole('status')` e `queryByRole('status')` (mais de um elemento é erro nos dois). Os dois usos existentes em `expenses-list.test.tsx` passam a localizar pelo texto: `getByText('Carregando gastos…')` com `toHaveAttribute('role', 'status')` em `shows the loading state`, e `queryByText('Carregando gastos…')` em `keeps the previous rows while the next page loads`. Os nomes dos testes não mudam.
- Linhas da tabela nos testes: `within(table).getAllByRole('row')` inclui a linha do `<thead className="sr-only">` (escondida só visualmente); as linhas de dados são `.slice(1)`, e as três células de cada uma vêm de `within(row).getAllByRole('cell')` na ordem rótulo, contagem, total. No Playwright, `table.getByRole('row')` também conta o cabeçalho: com dois grupos, `toHaveCount(3)` e os dados em `nth(1)` e `nth(2)`.
- Os mocks exportam, além de `filterExpenses(url)`, `groupByCategory(items: Expense[]): CategoryGroup[]`; o espião de `by-category` em `expenses-list.test.tsx` reaproveita os dois e só anota as chamadas. `filterForSpy` continua como está (dívida registrada na SPEC).
- Base padrão dos testes Python de `by-category` (`_BY_CATEGORY_ROWS`): `_transaction("g1", "2026-09-01", -60.0, categoria="Groceries", descricao="AÇOUGUE SÃO JORGE")`, `_transaction("g2", "2026-09-02", -84.9, categoria="Groceries", descricao="MERCADO DO BAIRRO")`, `_transaction("h1", "2026-09-03", -150.0, categoria="Housing", descricao="POSTO CENTRAL")`, `_transaction("n1", "2026-09-04", -50.0, categoria="", descricao="GASTO n1")`, `_transaction("c1", "2026-08-20", -45.0, categoria="Shopping", descricao="FARMACIA", conta_id="sync-acc-2")`, carregada com `accounts=[CREDIT_ACCOUNT]`. Resposta esperada sem filtro, em ordem: `("Housing", "Casa", 1, -15000)`, `("Groceries", "Supermercado", 2, -14490)`, `(None, "Sem categoria", 1, -5000)`, `("Shopping", "Compras", 1, -4500)`; `total_cents == -38990`.
- Números do mock (`fakeExpenses`, 45 gastos): sem filtro, "Compras · 42 gastos · R$ 9.484,90", "Alimentação · 1 gasto · R$ 440,00", "Sem categoria · 1 gasto · R$ 430,00", "Transporte · 1 gasto · R$ 420,00"; `?account=acc-credit-1` → só "Compras · 13 gastos · R$ 2.730,00" (ids 3, 6, …, 39).
- `playwright.config.ts` já tem `workers: 1`; o e2e novo entra no `e2e/expenses.spec.ts` existente, sob o `mode: 'serial'` que já está lá.

## Fase 1 — `GET /api/transactions/expenses/by-category` agrupando em SQL sobre o mesmo predicado da lista

Só Python. Ao final: `sum_by_category` em `app/queries/expenses.py` agrupa os gastos por `NULLIF(t.category, '')` sobre `_FROM` + `_where` (o mesmo `FROM/WHERE` de `_TOTAL`), ordena por `abs(total)` decrescente, resolve o rótulo pt-BR em Python; o router expõe `GET /expenses/by-category` com `from`, `to`, `account_id`, `q` e a mesma validação de `GET /expenses` (extraída em duas funções privadas usadas pelos dois handlers); o `/openapi.json` lista o caminho; e a suíte Python prova, entre outros, que a soma dos grupos é igual ao `total_cents` da lista para o mesmo filtro.

- [x] T1.1 — `CategoryTotal` e `sum_by_category` em `app/queries/expenses.py`
  - Arquivos: `app/queries/expenses.py` (alterar)
  - O que fazer:
    - `@dataclass(frozen=True) class CategoryTotal:` com `category: str | None`, `label: str`, `count: int`, `total_cents: int`, declarada logo depois de `ExpensesPage`.
    - Constante `_BY_CATEGORY = f"SELECT NULLIF(t.category, '') AS category, count(*) AS count, coalesce(sum(t.amount_cents), 0) AS total {_FROM}"` (uma linha só, ao lado de `_TOTAL`, coberta pelo mesmo comentário "Reason: the primary key (payee, source)…").
    - `def sum_by_category(conn: sqlite3.Connection, *, date_from: str | None = None, date_to: str | None = None, account_id: str | None = None, search: str | None = None) -> list[CategoryTotal]`: `where, params = _where(date_from, date_to, account_id, search)`; `rows = conn.execute(f"{_BY_CATEGORY} {where} GROUP BY NULLIF(t.category, '') ORDER BY abs(total) DESC, CASE WHEN NULLIF(t.category, '') IS NULL THEN 1 ELSE 0 END, NULLIF(t.category, '')", params).fetchall()`; `categories = category_labels()`; para cada linha, `category = row["category"]`, `label = "Sem categoria" if category is None else categories.get(category, category)`, e `CategoryTotal(category=category, label=label, count=row["count"], total_cents=int(row["total"]))`. Devolve a lista na ordem do SQL. Nenhum `commit`, nenhuma outra função muda.
  - Skills: —
  - Complexidade: baixa

- [x] T1.2 — Validação compartilhada e handler `expenses_by_category` em `app/routers/transactions.py`
  - Arquivos: `app/routers/transactions.py` (alterar)
  - O que fazer:
    - Importa `sum_by_category` de `app.queries.expenses` na mesma linha de `list_expenses`.
    - `class CategoryGroup(BaseModel):` com `category: str | None`, `label: str`, `count: int`, `total_cents: int`. `class CategoryTotalsResponse(BaseModel):` com `groups: list[CategoryGroup]`, `total_cents: int`. Declaradas depois de `ExpensesResponse`.
    - `def _date_bounds(from_: date | None, to: date | None) -> tuple[str | None, str | None]`: se `from_ is not None and to is not None and to < from_`, levanta `HTTPException(status_code=422, detail="A data final precisa ser igual ou posterior à inicial.")`; devolve `(from_.isoformat() if from_ else None, to.isoformat() if to else None)`.
    - `def _search_term(q: str | None) -> str | None`: `term = q.strip() if q is not None else ""`; devolve `term if len(term) >= 2 else None`.
    - `expenses` passa a usar as duas: `date_from, date_to = _date_bounds(from_, to)` e `search = _search_term(q)` antes de `connect()`; `list_expenses(..., date_from=date_from, date_to=date_to, account_id=account_id, search=search)`. Nada muda na assinatura, em `Expense` nem em `ExpensesResponse`.
    - `@router.get("/expenses/by-category")` `def expenses_by_category(from_: Annotated[date | None, Query(alias="from")] = None, to: Annotated[date | None, Query()] = None, account_id: Annotated[str | None, Query(min_length=1)] = None, q: Annotated[str | None, Query()] = None) -> CategoryTotalsResponse`: mesmas duas funções, `conn = connect()`, `try: groups = sum_by_category(conn, date_from=date_from, date_to=date_to, account_id=account_id, search=search) finally: conn.close()`; devolve `CategoryTotalsResponse(groups=[CategoryGroup(category=g.category, label=g.label, count=g.count, total_cents=g.total_cents) for g in groups], total_cents=sum(g.total_cents for g in groups))`. Rota `def`, não `async def`; sem `SELECT`, `INSERT` ou `commit(` no arquivo.
  - Skills: api-requests
  - Complexidade: média

- [x] T1.3 — Testes da fase 1
  - Arquivos: `tests/test_expenses_api.py` (alterar)
  - O que fazer (api-requests, unit-testing): reaproveitar `client`, `_sign_in`, `_transaction`, `_load`, `CREDIT_ACCOUNT`. Helper `_by_category(client: TestClient, query: str = "") -> dict[str, Any]` que faz `client.get(f"/api/transactions/expenses/by-category?{query}")` e devolve `response.json()`; helper `_group_tuples(body) -> list[tuple[str | None, str, int, int]]` com `(category, label, count, total_cents)` de cada grupo; constante `_BY_CATEGORY_ROWS` com a base padrão do cabeçalho. Salvo indicação, cada caso faz `_load(_BY_CATEGORY_ROWS, accounts=[CREDIT_ACCOUNT])` e `_sign_in(client)`. Casos:
    - `test_by_category_without_session_answers_401` — sem `_sign_in`, `GET /api/transactions/expenses/by-category` responde 401 com `{"detail": "nao autenticado"}`.
    - `test_an_empty_base_answers_no_groups` — sem `_load`; resposta 200 igual a `{"groups": [], "total_cents": 0}`.
    - `test_the_by_category_response_has_the_contract_fields` — `set(body) == {"groups", "total_cents"}` e cada grupo tem exatamente `{"category", "label", "count", "total_cents"}`.
    - `test_groups_come_biggest_first_with_the_seed_label_and_the_count` — `_group_tuples` é `[("Housing", "Casa", 1, -15000), ("Groceries", "Supermercado", 2, -14490), (None, "Sem categoria", 1, -5000), ("Shopping", "Compras", 1, -4500)]` e `total_cents == -38990`.
    - `test_empty_and_missing_categories_fall_into_a_single_uncategorised_group_ordered_by_total` — base própria: `_transaction("e1", "2026-09-01", -10.0, categoria="")`, `_transaction("e2", "2026-09-02", -20.0, categoria=None)`, `_transaction("s1", "2026-09-03", -40.0, categoria="Shopping")`, `_transaction("h1", "2026-09-04", -25.0, categoria="Housing")`; `_group_tuples` é `[("Shopping", "Compras", 1, -4000), (None, "Sem categoria", 2, -3000), ("Housing", "Casa", 1, -2500)]` (um só grupo nulo, no meio).
    - `test_a_category_outside_the_seed_uses_its_own_key_as_label` — base própria com `categoria="Desconhecida"` (-10.0); o único grupo é `("Desconhecida", "Desconhecida", 1, -1000)`.
    - `test_equal_totals_put_the_uncategorised_group_last_and_then_sort_by_key` — base própria: `categoria="Housing"` -10.0, `categoria=""` -10.0, `categoria="Groceries"` -10.0; as chaves vêm `["Groceries", "Housing", None]`.
    - `test_transfers_and_refunds_stay_out_of_the_groups` — base padrão mais `_transaction("t1", "2026-09-05", -500.0, categoria="Housing", eh_transferencia=True)` e `_transaction("r1", "2026-09-06", -70.0, categoria="Housing", eh_estorno=True)`; o grupo `Housing` continua `count == 1`, `total_cents == -15000`, e `total_cents` da resposta continua `-38990`.
    - `test_from_and_to_limit_the_groups` — `?from=2026-09-01&to=2026-09-30` devolve as chaves `["Housing", "Groceries", None]` e `total_cents == -34490`; `?to=2026-08-31` devolve só `[("Shopping", "Compras", 1, -4500)]`.
    - `test_account_id_limits_the_groups` — `?account_id=sync-acc-2` devolve só `Shopping`; `?account_id=desconhecida` devolve `groups == []` e `total_cents == 0`.
    - `test_q_limits_the_groups_and_a_short_q_is_ignored` — `?q=central` devolve só `("Housing", "Casa", 1, -15000)`; `?q=acougue` devolve só `("Groceries", "Supermercado", 1, -6000)`; `?q=a` devolve os mesmos quatro grupos da chamada sem `q`.
    - `test_an_inverted_interval_answers_422_on_by_category` — `?from=2026-09-30&to=2026-09-01` responde 422 com `detail == "A data final precisa ser igual ou posterior à inicial."`.
    - `test_an_empty_account_id_answers_422_on_by_category` — `?account_id=` responde 422.
    - `test_the_group_totals_add_up_to_the_list_total_for_the_same_filter` — para cada `query` em `["", "from=2026-09-01&to=2026-09-30", "account_id=sync-acc-2", "q=mercado", "from=2026-09-02&account_id=sync-acc-1&q=ma"]`: `body = _by_category(client, query)`; `sum(g["total_cents"] for g in body["groups"]) == body["total_cents"]` e `body["total_cents"] == client.get(f"/api/transactions/expenses?{query}").json()["total_cents"]`; também `sum(g["count"] ...) == ...["total"]`.
    - `test_the_openapi_lists_by_category_with_four_optional_parameters` — `GET /openapi.json`; `paths["/api/transactions/expenses/by-category"]["get"]["parameters"]` tem exatamente os nomes `{"from", "to", "account_id", "q"}`, todos com `in == "query"` e `required` ausente ou `False`.
  - Skills: api-requests, unit-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — `bash scripts/lint.sh` sai com código 0. (comando)
- [x] CA1.2 — `uv run pytest tests/test_expenses_api.py tests/test_db.py tests/test_sync_api.py tests/test_accounts_api.py tests/test_auth_api.py tests/test_ingest.py` passa; os 15 testes nomeados em T1.3 existem em `tests/test_expenses_api.py`, e os 60 já existentes (`test_expenses_without_session_answers_401` … `test_the_openapi_lists_q_as_an_optional_string`) continuam com o mesmo nome. (comando)
- [x] CA1.3 — `bash scripts/gates/gates_runner.sh` sai com código 0. (comando)
- [x] CA1.4 — `app/queries/expenses.py` define `@dataclass(frozen=True) class CategoryTotal` com os campos `category: str | None`, `label: str`, `count: int`, `total_cents: int`; define `_BY_CATEGORY` contendo `NULLIF(t.category, '') AS category`, `count(*) AS count`, `coalesce(sum(t.amount_cents), 0) AS total` e `{_FROM}`; define `def sum_by_category(conn: sqlite3.Connection, *, date_from: str | None = None, date_to: str | None = None, account_id: str | None = None, search: str | None = None) -> list[CategoryTotal]`, que chama `_where(date_from, date_to, account_id, search)` e executa SQL contendo `GROUP BY NULLIF(t.category, '')` e `ORDER BY abs(total) DESC, CASE WHEN NULLIF(t.category, '') IS NULL THEN 1 ELSE 0 END, NULLIF(t.category, '')`; contém `"Sem categoria" if category is None else`; `_FROM`, `_where`, `_TOTAL`, `_SELECT` e `list_expenses` mantêm o texto de hoje; o arquivo não contém `commit(`. (estrutural)
- [x] CA1.5 — `app/routers/transactions.py` define `def _date_bounds(from_: date | None, to: date | None) -> tuple[str | None, str | None]` (contendo `to < from_` e o texto "A data final precisa ser igual ou posterior à inicial.") e `def _search_term(q: str | None) -> str | None` (contendo `q.strip()` e `len(term) >= 2`); a string "A data final precisa ser igual ou posterior à inicial." aparece **uma** vez no arquivo e `q.strip()` uma vez; `class CategoryGroup(BaseModel)` tem `category: str | None`, `label: str`, `count: int`, `total_cents: int`; `class CategoryTotalsResponse(BaseModel)` tem `groups: list[CategoryGroup]` e `total_cents: int`; existe `@router.get("/expenses/by-category")` sobre `def expenses_by_category(` com os parâmetros `from_: Annotated[date | None, Query(alias="from")] = None`, `to: Annotated[date | None, Query()] = None`, `account_id: Annotated[str | None, Query(min_length=1)] = None`, `q: Annotated[str | None, Query()] = None` e retorno `-> CategoryTotalsResponse`; o handler chama `sum_by_category(` dentro de `try:` com `finally:` e `conn.close()`, e contém `sum(g.total_cents for g in groups)`; `def expenses(` chama `_date_bounds(from_, to)` e `_search_term(q)`; `Expense` e `ExpensesResponse` mantêm exatamente os campos de hoje; nenhuma rota é `async def`; `SELECT`, `INSERT` e `commit(` não aparecem no arquivo. (estrutural)
- [x] CA1.6 — Sem sessão o endpoint responde 401 (`test_by_category_without_session_answers_401`); base vazia responde `{"groups": [], "total_cents": 0}` (`test_an_empty_base_answers_no_groups`); os grupos vêm do maior `abs(total)` para o menor com o rótulo do seed e a contagem (`test_groups_come_biggest_first_with_the_seed_label_and_the_count`); `categoria=""` e ausente formam um único grupo `category: null` / `label: "Sem categoria"` ordenado pelo total (`test_empty_and_missing_categories_fall_into_a_single_uncategorised_group_ordered_by_total`); chave fora do seed vira o próprio rótulo (`test_a_category_outside_the_seed_uses_its_own_key_as_label`); empate ordena nulo por último e depois pela chave (`test_equal_totals_put_the_uncategorised_group_last_and_then_sort_by_key`); transferência e estorno ficam fora (`test_transfers_and_refunds_stay_out_of_the_groups`); `from`/`to`, `account_id` e `q` limitam e `q=a` é ignorado (`test_from_and_to_limit_the_groups`, `test_account_id_limits_the_groups`, `test_q_limits_the_groups_and_a_short_q_is_ignored`); `to < from` e `account_id=` respondem 422 (`test_an_inverted_interval_answers_422_on_by_category`, `test_an_empty_account_id_answers_422_on_by_category`); a soma dos grupos é igual ao `total_cents` de `GET /expenses` com o mesmo filtro (`test_the_group_totals_add_up_to_the_list_total_for_the_same_filter`); o OpenAPI lista o caminho com `from`, `to`, `account_id`, `q` opcionais (`test_the_openapi_lists_by_category_with_four_optional_parameters`). (comportamental)
- [x] CA1.7 — `uv run pytest --cov=app.queries.expenses --cov=app.routers.transactions --cov-report=term tests/test_expenses_api.py` reporta ≥ 80% em `app/queries/expenses.py` e `app/routers/transactions.py`. (comando)

## Fase 2 — Bloco "Por categoria" na página "Gastos", mocks, testes de componente e e2e

Ao final: a página "Gastos" mostra, entre a barra de controles e a lista, o bloco "Por categoria" (`<h2>` + tabela com rótulo, "N gasto(s)" e total, do maior para o menor), que obedece a período, conta e busca da URL por compartilhar o mesmo objeto de filtros da lista; com mais de 8 categorias mostra as 8 maiores e o botão "Mostrar todas (N)"/"Mostrar menos"; tem carregando, erro com "Tentar de novo" e some quando não há grupos, independentemente do estado da lista; o mock responde `by-category`; a jornada é provada com API mockada e, contra o FastAPI real, pelo Playwright.

- [ ] T2.1 — Tipos, `useCategoryTotals` e mock de `by-category`
  - Arquivos: `src/features/expenses/types/expense.ts` (alterar); `src/features/expenses/api/get-category-totals.ts` (criar); `src/testing/mocks/handlers.ts` (alterar)
  - O que fazer:
    - `expense.ts`, depois de `ExpensesQuery`: `export interface CategoryGroup { category: string | null; label: string; count: number; total_cents: number }`; `export interface CategoryTotalsResponse { groups: CategoryGroup[]; total_cents: number }`; `export type CategoryTotalsQuery = Pick<ExpensesQuery, 'from' | 'to' | 'account' | 'search'>`. O resto não muda.
    - `get-category-totals.ts` (mesmo padrão de `get-expenses.ts`): `export function getCategoryTotals(query: CategoryTotalsQuery): Promise<CategoryTotalsResponse>` monta `new URLSearchParams()` e faz `params.set('from', …)`, `params.set('to', …)`, `params.set('account_id', …)`, `params.set('q', …)` cada um só quando o campo `!== null`; chama `apiRequest<CategoryTotalsResponse>(\`/api/transactions/expenses/by-category?${params.toString()}\`)`. `export function categoryTotalsQueryOptions(query: CategoryTotalsQuery)` → `queryOptions({ queryKey: ['expenses', 'by-category', query], queryFn: () => getCategoryTotals(query), placeholderData: keepPreviousData })`. `export function useCategoryTotals(query: CategoryTotalsQuery)` → `useQuery(categoryTotalsQueryOptions(query))`.
    - `handlers.ts`: importa `CategoryGroup` junto com os tipos de `expense`. `export function filterExpenses(url: URL): Expense[]` — extraído do handler de `/api/transactions/expenses` com exatamente o `from/to/account_id/q` de hoje (inclusive `trim()` e `length >= 2` com `foldText`); o handler passa a chamá-lo. `export function groupByCategory(items: Expense[]): CategoryGroup[]` — agrupa por `item.category ?? null`, `label = category ?? 'Sem categoria'`, soma `amount_cents` em `total_cents` e conta em `count`, ordena por `Math.abs(total_cents)` decrescente com desempate `category` nulo por último e depois por `category` (ordem binária com `<`). Handler novo `http.get('/api/transactions/expenses/by-category', ({ request }) => …)` que usa os dois e devolve `{ groups, total_cents: groups.reduce((sum, g) => sum + g.total_cents, 0) }`. `fakeExpenses`, `foldText`, `sortExpenses` e os demais handlers não mudam.
  - Skills: api-requests, api-mocking
  - Complexidade: baixa

- [ ] T2.2 — `CategoryTotals`: tabela, "Mostrar todas (N)", carregando, erro, vazio; receita "Tabela de totais" no `docs/design.md`
  - Arquivos: `src/features/expenses/components/category-totals.tsx` (criar); `docs/design.md` (alterar)
  - O que fazer:
    - `export function CategoryTotals({ query }: { query: CategoryTotalsQuery }): React.JSX.Element | null`. Constante de módulo `VISIBLE_GROUPS = 8`. `const { data, isPending, isError, refetch } = useCategoryTotals(query)`; `const [expanded, setExpanded] = useState(false)`; `const headingId = useId()`.
    - Estados, cada um no lugar do bloco, logo abaixo da barra de controles: `isPending` → `<p role="status">` com o texto "Carregando totais por categoria…" (receita "Carregando"); `isError` → `<Alert message="Não foi possível carregar os totais por categoria." action={{ label: 'Tentar de novo', onClick: () => void refetch() }} />`; `data.groups.length === 0` → `return null`.
    - Com dados: `const groups = data.groups`; `const visible = expanded ? groups : groups.slice(0, VISIBLE_GROUPS)`. Estrutura (aparência pelo `docs/design.md` e pelo piso de `interface-design`; sem JSX copiado deste plano): `<section aria-labelledby={headingId}>` no espaçamento padrão entre blocos; `<h2 id={headingId}>` "Por categoria" com o estilo de subtítulo do `design.md`, mas `mt-6` no lugar de `mt-8` (o bloco fica entre a barra e a lista); `<table aria-labelledby={headingId}>` pela receita nova "Tabela de totais": `<thead>` escondido só visualmente (`sr-only`) com `<th scope="col">` "Categoria", "Gastos" e "Total"; `<tbody>` com uma `<tr key={group.category ?? ''}>` por grupo visível e três `<td>`: o `label` (texto principal, truncando), `${count} gasto` quando `count === 1` senão `${count} gastos` (secundário, à direita, `tabular-nums`, sem quebra de linha) e `formatMoney(Math.abs(group.total_cents))` (valor monetário em destaque, à direita, sem quebra de linha). Se `groups.length > VISIBLE_GROUPS`, abaixo da tabela um `Button` de `@/components/ui/button` com `variant="secondary"`, `aria-expanded={expanded}`, `onClick={() => setExpanded((value) => !value)}` e texto `Mostrar todas (${groups.length})` quando fechado ou "Mostrar menos" quando aberto. Nenhuma linha tem `<button>`, `<Link>`, `<a` ou `onClick`. Sem `<ul>`/`<li>` (os testes existentes contam `listitem` na página inteira). Sem `style={{`, sem `useSearchParams`.
    - `docs/design.md`, na tabela "Padrões acrescentados pelas entregas", linha nova: `| Tabela de totais | \`<table className="mt-3 w-full text-sm">\`; \`<thead className="sr-only">\`; \`<tbody className="divide-y divide-gray-200">\`; \`<tr>\` com \`<td className="min-w-0 truncate py-2 text-gray-900">\` (rótulo), \`<td className="whitespace-nowrap py-2 pl-4 text-right text-gray-600 tabular-nums">\` (contagem) e \`<td className="whitespace-nowrap py-2 pl-4 text-right tabular-nums font-medium text-gray-900">\` (total); botão secundário abaixo com \`mt-3\` | 008 |`. O componente usa exatamente essas classes.
  - Skills: interface-design, client-state, error-handling, component-robustness
  - Complexidade: média

- [ ] T2.3 — `ExpensesList` compartilha os filtros com `CategoryTotals` e monta o bloco depois da barra em todos os ramos
  - Arquivos: `src/features/expenses/components/expenses-list.tsx` (alterar)
  - O que fazer:
    - Importa `CategoryTotals` de `@/features/expenses/components/category-totals` e o tipo `CategoryTotalsQuery` de `@/features/expenses/types/expense`.
    - Depois de `accountKnown`: `const filters: CategoryTotalsQuery = { from, to, account: accountKnown ? account : null, search }`; `useExpenses({ page, sort, order, ...filters })` substitui o objeto literal atual (mesmos valores, mesma chave).
    - O `const controls = (…)` de hoje passa a se chamar `header` e vira um fragmento com a `<div className="mt-6 flex flex-wrap items-end gap-3">` (inalterada por dentro) seguida de `<CategoryTotals query={filters} />`. Os quatro ramos (`isPending`, `isError`, `data.total === 0`, dados) devolvem `{header}` no lugar de `{controls}`; nada mais muda neles, nem nos textos "Carregando gastos…", "Não foi possível carregar os gastos.", "Tentar de novo", "Nenhum gasto para esse filtro." e "Nenhum gasto registrado ainda.". `readSearch`, `writeSearch`, `writeAccount`, `writeSorting`, `commitPeriod`, os dois `useEffect` e o `onChange` da `<Pagination` não mudam. O arquivo continua sem `useState`.
  - Skills: client-state, interface-design
  - Complexidade: baixa

- [ ] T2.4 — Fixture e2e com chave do seed e jornada Playwright do bloco contra o FastAPI real
  - Arquivos: `tests/data/e2e_transactions.json` (alterar); `e2e/expenses.spec.ts` (alterar)
  - O que fazer:
    - `tests/data/e2e_transactions.json`: no objeto `e2e-t-5` (`FARMACIA CENTRAL`, -45.0, `2026-08-15`, `acc-fixture-2`), `"categoria_pluggy"` e `"categoria"` passam de `"Health"` para `"Healthcare"`. Nenhum outro campo ou objeto muda.
    - `e2e/expenses.spec.ts`: teste novo `shows the totals by category and hides them when the filter has no spending`, dentro do `mode: 'serial'` existente, antes de `goes back to the balances page`: login com `LOGIN`/`PASSWORD` (mesmos quatro passos dos testes existentes) → `page.goto('/app/expenses?month=2026-08')` → `expect(page.getByRole('heading', { level: 2, name: 'Por categoria' })).toBeVisible()`; `const table = page.getByRole('table', { name: 'Por categoria' })`; `const rows = table.getByRole('row')`; `expect(rows).toHaveCount(3)` (cabeçalho + 2); `expect(rows.nth(1)).toContainText('Supermercado')`, `toContainText('1 gasto')`, `toContainText('R$ 60,00')`; `expect(rows.nth(2)).toContainText('Plano de saúde')` e `toContainText('R$ 45,00')`; `expect(page.getByRole('button', { name: /Mostrar todas/ })).toHaveCount(0)`; `expect(page.getByRole('listitem')).toHaveCount(2)` (a tabela não entra na contagem). Depois `page.goto('/app/expenses?q=zzzz')` → `expect(page.getByRole('heading', { level: 2, name: 'Por categoria' })).toHaveCount(0)`, `expect(page.getByRole('table')).toHaveCount(0)` e `expect(page.getByText('Nenhum gasto para esse filtro.')).toBeVisible()`. Os testes existentes não mudam.
  - Skills: e2e-testing
  - Complexidade: baixa

- [ ] T2.5 — Testes da fase 2
  - Arquivos: `src/features/expenses/components/__tests__/category-totals.test.tsx` (criar); `src/features/expenses/components/__tests__/expenses-list.test.tsx` (alterar)
  - O que fazer:
    - `category-totals.test.tsx` (component-testing, api-mocking): renderiza `<CategoryTotals query={…} />` com `renderWithProviders` de `@/testing/test-utils`; `const ALL: CategoryTotalsQuery = { from: null, to: null, account: null, search: null }`; helper `groupsOf(n: number): CategoryGroup[]` que gera `{ category: \`Cat ${i}\`, label: \`Categoria ${i}\`, count: i, total_cents: -1000 * (n - i + 1) }` para `i` de 1 a `n`; helper `respondWith(groups, options?)` que faz `server.use(http.get('/api/transactions/expenses/by-category', () => HttpResponse.json({ groups, total_cents: soma })))`; helper `dataRows()` = `within(screen.getByRole('table')).getAllByRole('row').slice(1)` e `cellsOf(row)` = `within(row).getAllByRole('cell')`. Casos:
      - `shows the heading and one row per group in the order received` — grupos `[{ category: 'Compras', label: 'Compras', count: 42, total_cents: -948490 }, { category: 'Transporte', label: 'Transporte', count: 1, total_cents: -42000 }]`; `findByRole('heading', { level: 2, name: 'Por categoria' })`; `getByRole('table', { name: 'Por categoria' })`; duas linhas de dados; células da primeira `['Compras', '42 gastos', 'R$ 9.484,90']` e da segunda `['Transporte', '1 gasto', 'R$ 420,00']` (via `toHaveTextContent`); nenhum `button` dentro da tabela; `queryAllByRole('listitem')` vazio.
      - `renders nothing when there are no groups` — `groups: []`; depois de `waitFor` da chamada (espia com `server.use` que marca `called = true`), `queryByRole('heading')`, `queryByRole('table')` e `queryByRole('status')` são `null` e `container` está vazio (`container.firstChild === null`).
      - `shows no button with eight groups` — `groupsOf(8)`; 8 linhas de dados; `queryByRole('button')` é `null`.
      - `shows only the eight biggest groups and "Mostrar todas (12)" with twelve` — `groupsOf(12)`; 8 linhas; `getByRole('button', { name: 'Mostrar todas (12)' })` com `aria-expanded="false"`; "Categoria 9" ausente.
      - `"Mostrar todas" reveals every group and turns into "Mostrar menos"` — `groupsOf(12)`; `userEvent.click` no botão → 12 linhas, `getByRole('button', { name: 'Mostrar menos' })` com `aria-expanded="true"`.
      - `"Mostrar menos" hides the extra groups again` — depois de abrir, clicar "Mostrar menos" → 8 linhas e o botão volta a "Mostrar todas (12)".
      - `shows the loading state` — handler com `await delay('infinite')`; `getByText('Carregando totais por categoria…')` tem `role="status"`; sem `table`.
      - `shows the error and retries` — primeira resposta `HttpResponse.json({}, { status: 500 })`, depois `groupsOf(2)`; `findByRole('alert')` com "Não foi possível carregar os totais por categoria." e `getByRole('button', { name: 'Tentar de novo' })`; clicar → `findByRole('table')` com 2 linhas e `queryByRole('alert')` nulo.
      - `uses the label received for the uncategorised group` — grupos `[{ category: null, label: 'Sem categoria', count: 1, total_cents: -43000 }, { category: 'Não classificado', label: 'Sem categoria', count: 1, total_cents: -100 }]`; duas linhas com "Sem categoria" e nenhum aviso no console (`vi.spyOn(console, 'error')` não chamado).
      - `sends from, to, account_id and q only when they are set` — espião que anota `url.searchParams`; `query = { from: '2026-08-01', to: '2026-08-31', account: 'acc-credit-1', search: 'mercado' }` → a chamada tem `from=2026-08-01`, `to=2026-08-31`, `account_id=acc-credit-1`, `q=mercado` e nenhuma outra chave; com `ALL` → `searchParams.size === 0`.
    - `expenses-list.test.tsx` (component-testing, api-mocking): importa `filterExpenses`, `groupByCategory` de `@/testing/mocks/handlers`; ajusta os dois localizadores de `status` conforme o cabeçalho (`shows the loading state`, `keeps the previous rows while the next page loads`); novo `spyOnCategoryTotalsRequests(): URLSearchParams[]` que faz `server.use(http.get('/api/transactions/expenses/by-category', ({ request }) => { const url = new URL(request.url); calls.push(url.searchParams); const groups = groupByCategory(filterExpenses(url)); return HttpResponse.json({ groups, total_cents: … }) }))`; helper `categoryRows()` como em `category-totals.test.tsx`. Os 58 testes existentes continuam com o mesmo nome. Casos novos:
      - `shows the totals by category above the list` — `route: '/expenses'`; `findByRole('heading', { level: 2, name: 'Por categoria' })`; quatro linhas de dados com células, nesta ordem, `['Compras', '42 gastos', 'R$ 9.484,90']`, `['Alimentação', '1 gasto', 'R$ 440,00']`, `['Sem categoria', '1 gasto', 'R$ 430,00']`, `['Transporte', '1 gasto', 'R$ 420,00']`; `queryByRole('button', { name: /Mostrar todas/ })` nulo; a `<section>` vem antes da `<ul>` no DOM (`section.compareDocumentPosition(list) & Node.DOCUMENT_POSITION_FOLLOWING`); `findAllByRole('listitem')` tem 20 itens.
      - `filters the totals by account and sends account_id to by-category` — `route: '/expenses?account=acc-credit-1'` com `spyOnCategoryTotalsRequests`; uma linha `['Compras', '13 gastos', 'R$ 2.730,00']`; a última chamada tem `account_id=acc-credit-1`.
      - `hides the totals block for a search without results` — `route: '/expenses?q=zzzz'`; `findByText('Nenhum gasto para esse filtro.')`; `queryByRole('heading', { level: 2 })` e `queryByRole('table')` nulos.
      - `keeps the list when by-category fails` — `server.use` com 500 só em `by-category`; `findByRole('alert')` com "Não foi possível carregar os totais por categoria."; `findAllByRole('listitem')` com 20 itens; `queryByText('Não foi possível carregar os gastos.')` nulo.
      - `"Próxima" does not refetch by-category` — `route: '/expenses'` com os dois espiões; após a carga, `categoryCalls.length === 1`; clicar "Próxima"; esperar a chamada de `/expenses` com `page=2`; `categoryCalls.length` continua `1`.
      - `changing the period refetches by-category with from and to` — `route: '/expenses?month=2026-08'` com `spyOnCategoryTotalsRequests`; a última chamada tem `from=2026-08-01` e `to=2026-08-31`; clicar "Todo o período" → nova chamada sem `from` nem `to`.
  - Skills: component-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 2

- [ ] CA2.1 — `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com código 0; `bash scripts/lint.sh`, `uv run pytest` e `bash scripts/gates/gates_runner.sh` também. (comando)
- [ ] CA2.2 — `pnpm test:e2e` sai com código 0 com o teste `shows the totals by category and hides them when the filter has no spending` em `e2e/expenses.spec.ts`, além dos seis já existentes ali (`opens the expenses page and lists only the spending` … `goes back to the balances page`) e dos de `e2e/sync.spec.ts` e `e2e/login-and-balances.spec.ts`; `playwright.config.ts` contém `workers: 1`; `e2e/expenses.spec.ts` contém `test.describe.configure({ mode: 'serial' })`, `getByRole('heading', { level: 2, name: 'Por categoria' })`, `getByRole('table', { name: 'Por categoria' })`, `'Supermercado'`, `'R$ 60,00'`, `'Plano de saúde'`, `'R$ 45,00'`, `/Mostrar todas/` e `q=zzzz`, e não contém `'Health'`; `tests/data/e2e_transactions.json` continua com seis objetos, o de `"id": "e2e-t-5"` com `"categoria_pluggy": "Healthcare"` e `"categoria": "Healthcare"`, e o arquivo não contém `"Health"`; `tests/data/e2e_accounts.json` e `scripts/e2e-backend.sh` não mudam. (comando, estrutural)
- [ ] CA2.3 — `src/features/expenses/types/expense.ts` exporta `interface CategoryGroup` com `category: string | null`, `label: string`, `count: number`, `total_cents: number`; `interface CategoryTotalsResponse` com `groups: CategoryGroup[]` e `total_cents: number`; `type CategoryTotalsQuery = Pick<ExpensesQuery, 'from' | 'to' | 'account' | 'search'>`. `src/features/expenses/api/get-category-totals.ts` exporta `getCategoryTotals(query: CategoryTotalsQuery): Promise<CategoryTotalsResponse>`, `categoryTotalsQueryOptions(query: CategoryTotalsQuery)` e `useCategoryTotals(query: CategoryTotalsQuery)`; contém `queryKey: ['expenses', 'by-category', query]`, `placeholderData: keepPreviousData`, `/api/transactions/expenses/by-category?`, `params.set('from'`, `params.set('to'`, `params.set('account_id'`, `params.set('q'`, cada `set` guardado por `!== null`; não contém `page`. `src/features/expenses/api/get-expenses.ts` não muda. (estrutural)
- [ ] CA2.4 — `src/features/expenses/components/category-totals.tsx` exporta `CategoryTotals({ query }: { query: CategoryTotalsQuery }): React.JSX.Element | null`; contém `useCategoryTotals(query)`, `useState(false)`, `useId()`, `VISIBLE_GROUPS = 8`, `slice(0, VISIBLE_GROUPS)`, `return null`, `role="status"` junto de "Carregando totais por categoria…", `Alert` de `@/components/ui/alert` com `message="Não foi possível carregar os totais por categoria."` e `label: 'Tentar de novo'`, `<section` com `aria-labelledby={headingId}`, `<h2 id={headingId}` com o texto "Por categoria", `<table` com `aria-labelledby={headingId}`, `<thead className="sr-only">` com `<th scope="col">` "Categoria", "Gastos" e "Total", `key={group.category ?? ''}`, `formatMoney(Math.abs(`, os textos `gasto` e `gastos` escolhidos por `count === 1`, `Button` de `@/components/ui/button` com `variant="secondary"` e `aria-expanded={expanded}`, e os textos `Mostrar todas (` e "Mostrar menos"; não contém `<ul`, `<li`, `<a `, `<Link`, `onClick` dentro de `<td`, `useSearchParams`, `style={{` nem `!important`. (estrutural)
- [ ] CA2.5 — `src/features/expenses/components/expenses-list.tsx` importa `CategoryTotals` de `@/features/expenses/components/category-totals`; define `const filters: CategoryTotalsQuery = { from, to, account: accountKnown ? account : null, search }`; chama `useExpenses({ page, sort, order, ...filters })`; renderiza `<CategoryTotals query={filters} />` **uma** vez, logo depois da `<div className="mt-6 flex flex-wrap items-end gap-3">` e dentro do mesmo `header` que é devolvido nos quatro ramos (`{header}` aparece quatro vezes; `{controls}` não aparece); a barra continua antes do primeiro `if (isPending)`; o arquivo não contém `useState`; mantém "Carregando gastos…", "Não foi possível carregar os gastos.", "Tentar de novo", "Nenhum gasto para esse filtro." e "Nenhum gasto registrado ainda."; `readSearch`, `writeSearch`, `writeAccount`, `writeSorting` e `commitPeriod` mantêm o texto de hoje. (estrutural)
- [ ] CA2.6 — Piso visual, lido no código: `docs/design.md` tem a linha "Tabela de totais" na tabela "Padrões acrescentados pelas entregas" com fatia `008`; em `category-totals.tsx` o `<h2>` usa `text-lg font-semibold` com `mt-6`; a `<table>` usa `mt-3 w-full text-sm`, o `<tbody>` `divide-y divide-gray-200`, o `<td>` do rótulo `min-w-0 truncate py-2 text-gray-900`, o da contagem `whitespace-nowrap py-2 pl-4 text-right text-gray-600 tabular-nums` e o do total `whitespace-nowrap py-2 pl-4 text-right tabular-nums font-medium text-gray-900`; o botão "Mostrar todas"/"Mostrar menos" é o `Button` com `variant="secondary"` num bloco com `mt-3`; o carregando usa `mt-6 text-gray-600`; nenhum arquivo em `src/features/expenses/` contém `style={{`, `<a href` ou `!important`. (estrutural)
- [ ] CA2.7 — `src/testing/mocks/handlers.ts` exporta `filterExpenses(url: URL): Expense[]` e `groupByCategory(items: Expense[]): CategoryGroup[]`; o handler de `/api/transactions/expenses` chama `filterExpenses(`; existe `http.get('/api/transactions/expenses/by-category'` que chama `groupByCategory(filterExpenses(` e devolve `groups` e `total_cents`; `groupByCategory` contém `?? 'Sem categoria'` e `Math.abs(`; `fakeExpenses` continua com 45 itens e `foldText`, `sortExpenses` e os demais handlers não mudam. (estrutural)
- [ ] CA2.8 — O bloco mostra o `<h2>` "Por categoria" e uma linha por grupo na ordem recebida com rótulo, "1 gasto"/"42 gastos" e o valor sem sinal (`shows the heading and one row per group in the order received`); some sem grupos (`renders nothing when there are no groups`); com 8 grupos não há botão e com 12 mostra 8 linhas e "Mostrar todas (12)", que abre para 12 com "Mostrar menos" e fecha de novo (`shows no button with eight groups`, `shows only the eight biggest groups and "Mostrar todas (12)" with twelve`, `"Mostrar todas" reveals every group and turns into "Mostrar menos"`, `"Mostrar menos" hides the extra groups again`); carregando e erro com "Tentar de novo" (`shows the loading state`, `shows the error and retries`); `category: null` usa o `label` recebido (`uses the label received for the uncategorised group`); a chamada leva só os filtros não nulos (`sends from, to, account_id and q only when they are set`). Na página: o bloco fica acima da lista com os quatro grupos do mock e a lista segue com 20 `listitem` (`shows the totals by category above the list`); `?account=acc-credit-1` reduz a "Compras · 13 gastos · R$ 2.730,00" e envia `account_id` (`filters the totals by account and sends account_id to by-category`); `?q=zzzz` esconde o bloco e mostra "Nenhum gasto para esse filtro." (`hides the totals block for a search without results`); 500 só em `by-category` mantém a lista (`keeps the list when by-category fails`); "Próxima" não refaz `by-category` (`"Próxima" does not refetch by-category`); trocar o período refaz com `from`/`to` (`changing the period refetches by-category with from and to`). (comportamental)
- [ ] CA2.9 — Os testes nomeados em T2.5 existem em `src/features/expenses/components/__tests__/category-totals.test.tsx` e `src/features/expenses/components/__tests__/expenses-list.test.tsx`; os 58 já existentes em `expenses-list.test.tsx` (`shows the loading state` … `retrying keeps the search`) continuam com o mesmo nome, e `shows the loading state` e `keeps the previous rows while the next page loads` localizam "Carregando gastos…" pelo texto, não por `getByRole('status')`/`queryByRole('status')`; `npx vitest run --coverage` reporta ≥ 80% de linhas em `src/features/expenses/api/get-category-totals.ts`, `src/features/expenses/components/category-totals.tsx` e `src/features/expenses/components/expenses-list.tsx`. (comando)

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
