# PLAN 015 — marcar não-gasto

Branch: `feature/015-marcar-nao-gasto`

Decisões registradas aqui (a SPEC deixou ao plano, ou o plano encontrou ao ler o código; escolhido o mais simples):

- **As três fases seguem o corte da SPEC**: 1) migração `022`, predicado único em `spending.py` (dívida 025), `view` nas consultas e `set/clear_not_expense` no domínio, com os testes Python de cada módulo; 2) rotas `PUT`/`DELETE …/not-expense`, `view` nos dois `GET`, os mocks MSW e `tests/test_expenses_api.py`; 3) front, e2e, `design.md` e roadmap. O campo `Expense.not_expense_reason` (Pydantic) entra na fase 2 com o router — na fase 1 o dicionário de `_item` já traz a chave, e o Pydantic ignora chave extra, então o contrato HTTP não muda antes da hora.
- **`tests/test_migrations.py` conta migrações por fatia relativa** (`EXPECTED_MIGRATIONS[:-1]`, `[:-2]`, `[:-3]`): ao entrar `022_not_expense.sql` no fim da lista, as três fatias dos testes de `019`, `020` e `021` descem uma posição (`[:-4]`, `[:-3]`, `[:-2]`), senão cada um passa a aplicar a migração que devia estar faltando.
- **O tipo `Expense` do front ganha `not_expense_reason` já na fase 2**, junto com `fakeExpenses`: `generateFakeExpenses(): Expense[]` devolve literais, e uma chave a mais sem o tipo reprova o `typecheck`. `BASE: Expense` em `category-picker.test.tsx` ganha `not_expense_reason: null` pelo mesmo motivo. `ExpenseView`, `ExpensesQuery.view` e `CategoryTotalsQuery` ficam para a fase 3, quando `get-expenses.ts` e `expenses-list.tsx` passam a mandar `view`.
- **O mock de `month-signal` não muda**: ele soma `filterExpenses(url)`, e `filterExpenses` passa a tratar `view` ausente como `expenses`; a rota de `month-signal` nunca recebe `view`, então o mock já soma só os gastos.
- **`readView`/`writeView` não são exportados** (mesma forma de `readSort`/`writeSorting`); o caso `readView('x') → 'expenses'` da SPEC é provado por integração: `route: '/expenses?view=x'` renderiza o modo "Gastos" com o `<select>` "Mostrar" em `expenses`.
- **A rota `DELETE` do router usa `@router.delete` sobre função `def` que devolve `Expense` com 200** (o padrão de `update_category`); nada de 204.
- **`test_route_guard.py::test_every_registered_route_requires_session` já cobre `PUT`/`DELETE` novos**: o guard é global e o teste percorre as rotas registradas.
- **O teste e2e novo entra em `e2e/expenses.spec.ts`** (arquivo em `mode: 'serial'`; `playwright.config.ts` tem `workers: 1`), depois de `shows the month against its ceiling, edits the ceiling inline and leaves the base as it found it` e antes de `goes back to the balances page`, e deixa a base como encontrou (desmarca no fim). Números de `tests/data/e2e_transactions.json`: 2026-08 tem AÇOUGUE SÃO JORGE −60,00 (Supermercado) e FARMACIA CENTRAL −45,00 (Plano de saúde) = R$ 105,00.
- **`docs/roadmap.md`**: a linha `| 025 | \`spending-filter-predicate-unification\` |` passa de `planned` a `done` na fase 1 (T1.2), que a fecha; a linha `| 015 | \`marcar-nao-gasto\` |` passa de `in-progress` a `in-review` na fase 3 (T3.4), ao abrir o PR.

## Fase 1 — Migração `022`, predicado único (`OUTFLOW`/`SPENDING`/`EXCLUDED`/`date_window`), `view` nas consultas, `set/clear_not_expense` e os testes Python

Só Python (mais uma linha de `docs/roadmap.md`). Ao final: `transactions.not_expense_reason` existe (nula = gasto normal) e sobrevive à reingestão; `SPENDING` exclui linha marcada em todos os quinze consumidores e em `residue`; `total_spending_cents` e `_where` compõem a janela de data por `date_window`; `list_expenses`/`sum_expenses`/`sum_by_category` aceitam `view="excluded"`; `set_not_expense`/`clear_not_expense` gravam e limpam o motivo com as recusas do domínio.

- [x] T1.1 — Migração `022` e a numeração
  - Arquivos: `app/migrations/sql/022_not_expense.sql` (criar); `app/migrations/NUMBERING.md` (alterar)
  - O que fazer:
    - `022_not_expense.sql`, uma linha, no molde de `021_category_limit.sql`: `ALTER TABLE transactions ADD COLUMN not_expense_reason TEXT NULL CHECK (not_expense_reason IS NULL OR not_expense_reason IN ('own_transfer', 'refund', 'other'));`
    - `NUMBERING.md`: o trecho "a próxima migração é `022`" passa a "a próxima migração é `023`". Nada mais muda.
    - `app/ingest/loader.py` **não muda**: `not_expense_reason` fica fora de `_TRANSACTION_COLUMNS`, e é isso que preserva a marcação no `ON CONFLICT DO UPDATE` (o mesmo mecanismo de `category_source`).
  - Skills: —
  - Complexidade: baixa

- [x] T1.2 — Predicado único e janela de data em `app/queries/spending.py`; `classify.py` importa em vez de copiar; dívida 025 fechada no roadmap
  - Arquivos: `app/queries/spending.py` (alterar); `app/taxonomy/classify.py` (alterar); `docs/roadmap.md` (alterar)
  - O que fazer:
    - `spending.py`: `OUTFLOW = "amount_cents < 0 AND is_transfer = 0 AND is_refund = 0 AND refunded_by IS NULL"`; `SPENDING = f"{OUTFLOW} AND not_expense_reason IS NULL"`; `EXCLUDED = f"{OUTFLOW} AND not_expense_reason IS NOT NULL"` (colunas sem alias, como hoje). Função nova `def date_window(date_from: str | None, date_to: str | None, column: str = "date") -> tuple[str, list[str]]`: devolve `("", [])` sem datas; acrescenta `f" AND {column} >= ?"` com `date_from` e `f" AND {column} <= ?"` com `date_to`, nessa ordem. `total_spending_cents(conn, start=None, end=None) -> int` mantém a assinatura e passa a fazer `window, params = date_window(start, end)` e executar `f"{_TOTAL_SPENDING}{window}"`; os `if start is not None` / `if end is not None` do corpo somem. O comentário de topo (invariante 25) fica; o módulo continua importando só `sqlite3`.
    - `classify.py`: apaga a constante `_SPENDING` e o comentário acima dela; `from app.queries.spending import SPENDING`; `residue` usa `{SPENDING}` na f-string. Nada mais muda (sem ciclo: `spending.py` não importa de `app.taxonomy`).
    - `docs/roadmap.md`: na linha `| 025 | \`spending-filter-predicate-unification\` |`, o status `planned` passa a `done`. Nenhuma outra linha muda nesta fase.
  - Skills: —
  - Complexidade: baixa

- [x] T1.3 — `view` nas consultas de `app/queries/expenses.py` e o motivo no item
  - Arquivos: `app/queries/expenses.py` (alterar)
  - O que fazer:
    - Importa `EXCLUDED, SPENDING, date_window` de `app.queries.spending`. `View = Literal["expenses", "excluded"]` ao lado de `Sort`/`Order`; `_PREDICATE: dict[View, str] = {"expenses": SPENDING, "excluded": EXCLUDED}`.
    - `_where(view: View, date_from: str | None, date_to: str | None, account_id: str | None, search: str | None) -> tuple[str, list[str]]`: `sql = f"WHERE {_PREDICATE[view]}"`; `window, params = date_window(date_from, date_to, "t.date")`; `sql += window`; depois conta e busca como hoje. Os dois `if date_from`/`if date_to` do corpo somem: a janela tem um dono só.
    - `_SELECT` ganha `t.not_expense_reason` na lista de colunas (antes de `t.amount_cents`, ou no fim: tanto faz — o `_item` lê por nome). `_item` devolve também `"not_expense_reason": row["not_expense_reason"]`.
    - `sum_expenses`, `list_expenses` e `sum_by_category` ganham `view: View = "expenses"` (keyword-only, como os demais) e repassam a `_where(view, ...)`; `list_expenses` repassa `view` a `sum_expenses`. `get_expense` continua sem predicado.
  - Skills: —
  - Complexidade: baixa

- [x] T1.4 — `set_not_expense`/`clear_not_expense` em `app/taxonomy/override.py`
  - Arquivos: `app/taxonomy/override.py` (alterar)
  - O que fazer: `from app.queries.spending import OUTFLOW`; `Reason = Literal["own_transfer", "refund", "other"]`; `REASONS: frozenset[str] = frozenset({"own_transfer", "refund", "other"})`; `class NotAnOutflowError(ValueError)` com `__init__(self, transaction_id: object)` guardando `self.transaction_id` e mensagem `f"lançamento não é uma saída que conta como gasto: {transaction_id}"`. `def set_not_expense(conn: sqlite3.Connection, transaction_id: int, reason: Reason) -> None`: `_require(conn, transaction_id)`; `conn.execute(f"SELECT 1 FROM transactions WHERE id = ? AND {OUTFLOW}", (transaction_id,)).fetchone()` — `None` → `raise NotAnOutflowError(transaction_id)`; `_write(conn, "UPDATE transactions SET not_expense_reason = ? WHERE id = ?", (reason, transaction_id))`. `def clear_not_expense(conn: sqlite3.Connection, transaction_id: int) -> None`: `_require`; `_write(conn, "UPDATE transactions SET not_expense_reason = NULL WHERE id = ?", (transaction_id,))`. `_require` e `_write` não mudam; o `classify_all` que `_write` dispara é aceito (SPEC, dívida registrada).
  - Skills: —
  - Complexidade: baixa

- [x] T1.5 — Testes da fase 1
  - Arquivos: `tests/test_migrations.py` (alterar); `tests/test_spending.py` (alterar); `tests/test_override.py` (alterar); `tests/test_ingest.py` (alterar)
  - O que fazer (unit-testing):
    - `tests/test_migrations.py`: `EXPECTED_MIGRATIONS` ganha `"022_not_expense.sql"` no fim; as fatias `EXPECTED_MIGRATIONS[:-3]` (teste da `019`), `[:-2]` (`020`) e `[:-1]` (`021`) viram `[:-4]`, `[:-3]` e `[:-2]`; `test_a_fresh_base_applies_the_nineteen_real_migrations` é renomeado para `test_a_fresh_base_applies_the_twenty_real_migrations` e afirma `"migrations applied: 20" in result.stdout`. Teste novo, no molde do da `021`:
      - `test_a_base_migrated_before_022_gets_a_null_reason_that_only_accepts_the_three_values` — escreve `EXPECTED_MIGRATIONS[:-1]`, aplica, insere uma linha em `transactions` (colunas mínimas: `pluggy_id`, `account_id`, `date`, `amount_cents`, `type`, mais as `NOT NULL` que o esquema exigir), escreve `022_not_expense.sql` e aplica; `pragma_table_info('transactions')` tem `not_expense_reason` com `type == "TEXT"` e `notnull == 0`; o valor da linha é `None`; `UPDATE transactions SET not_expense_reason = 'own_transfer'`, `'refund'` e `'other'` passam; `'x'` levanta `sqlite3.IntegrityError` com `match="CHECK constraint failed"`; `NULL` volta a passar.
    - `tests/test_spending.py` (fixture `loaded` existente; importa `date_window` também):
      - `test_a_row_marked_as_not_expense_leaves_the_total` — `UPDATE transactions SET not_expense_reason = 'own_transfer' WHERE pluggy_id = 't-mercado'`, `commit`; `total_spending_cents(loaded) == -1050`; `total_spending_cents(loaded, start="2025-09-01") == 0`.
      - `test_date_window_without_dates_is_empty` — `date_window(None, None) == ("", [])`.
      - `test_date_window_composes_the_column_and_the_bounds_in_order` — `date_window("2025-09-01", None, "t.date") == (" AND t.date >= ?", ["2025-09-01"])`; `date_window(None, "2025-09-30") == (" AND date <= ?", ["2025-09-30"])`; `date_window("2025-09-01", "2025-09-30") == (" AND date >= ? AND date <= ?", ["2025-09-01", "2025-09-30"])`.
    - `tests/test_override.py` (fixtures `taxonomy_conn`, `seed`, helper `prepared`; importa `NotAnOutflowError`, `clear_not_expense`, `set_not_expense` e `residue` de `app.taxonomy.classify`; helper `reason_of(conn, id) -> str | None` lendo `not_expense_reason`):
      - `test_set_not_expense_writes_the_reason_and_reclassifies` — `set_not_expense(conn, id, "refund")`; `reason_of == "refund"`; `state(conn, id)[3] == "Saúde"` (o `classify_all` rodou); reaberta a base (`connect(path)` como em `test_set_manual_writes_the_category_marks_it_manual_and_reclassifies`), o valor persiste (`commit` feito).
      - `test_clear_not_expense_returns_to_null` — marca e limpa; `reason_of is None`.
      - `test_clear_not_expense_on_a_row_already_null_does_not_fail` — `clear_not_expense` sem marcar; `reason_of is None`.
      - `test_set_and_clear_not_expense_refuse_an_unknown_transaction` — `id + 1000` → `UnknownTransactionError` nas duas.
      - `test_set_not_expense_refuses_a_transfer_a_refund_and_an_income` — carrega mais três linhas (`eh_transferencia=True`; `eh_estorno=True` com `valor` positivo; `valor=100.0` sem marcas); para cada uma `set_not_expense(conn, id, "other")` levanta `NotAnOutflowError` com `.transaction_id == id` e `reason_of is None`.
      - `test_the_residue_stops_counting_a_row_marked_as_not_expense` — na base de `prepared` (regra de categoria cobre `Healthcare`), carrega uma segunda linha sem categoria (`categoria=""`, −20,00, `2026-09-02`) e `classify_all`; `residue(conn, start="2026-09-01", end="2026-09-30")["entries"] == 1`; `set_not_expense` nela com `"other"`; `entries == 0` e `amount_cents == 0`.
    - `tests/test_ingest.py` (fixtures `conn`, `accounts`, fixture `transacoes_id_duplicado.json`, helper `row`):
      - `test_a_new_row_enters_with_a_null_not_expense_reason` — após a primeira ingestão, `SELECT not_expense_reason … WHERE pluggy_id = 'fix-duplicada'` é `None`.
      - `test_reingesting_keeps_the_not_expense_reason_and_still_updates_the_description` — `UPDATE transactions SET not_expense_reason = 'refund' WHERE pluggy_id = 'fix-duplicada'`, `commit`; `transactions[0]["descricao"] = "DESCRICAO NOVA"`; reingere; a linha tem `not_expense_reason == "refund"` e `description == "DESCRICAO NOVA"`.
  - Skills: unit-testing
  - Complexidade: média

### Critérios de aceite da fase 1

- [x] CA1.1 — `bash scripts/lint.sh` sai com código 0; `uv run pytest` passa; `bash scripts/gates/gates_runner.sh` sai com código 0; `pnpm typecheck` e `pnpm test` continuam com código 0 (nada de `src/` muda nesta fase). (comando)
- [x] CA1.2 — `app/migrations/sql/022_not_expense.sql` existe, tem uma única instrução `ALTER TABLE transactions ADD COLUMN not_expense_reason TEXT NULL CHECK (…)` cujo `CHECK` cita `'own_transfer'`, `'refund'` e `'other'`; `app/migrations/NUMBERING.md` contém "a próxima migração é `023`" e não contém "é `022`"; `app/ingest/loader.py` não muda (`git diff develop -- app/ingest/loader.py` vazio) e a string `not_expense_reason` não aparece nele. (estrutural)
- [x] CA1.3 — `app/queries/spending.py` define `OUTFLOW`, `SPENDING = f"{OUTFLOW} AND not_expense_reason IS NULL"`, `EXCLUDED = f"{OUTFLOW} AND not_expense_reason IS NOT NULL"` e `def date_window(date_from: str | None, date_to: str | None, column: str = "date") -> tuple[str, list[str]]`; o corpo de `total_spending_cents` chama `date_window(` e não contém `" AND date >= ?"` literal; o arquivo importa só `sqlite3`. `app/taxonomy/classify.py` não contém `_SPENDING`, importa `SPENDING` de `app.queries.spending` e `residue` usa `{SPENDING}`. `rg -n "amount_cents < 0 AND is_transfer = 0" app/` devolve exatamente uma linha, em `app/queries/spending.py`. (estrutural)
- [x] CA1.4 — `app/queries/expenses.py` define `View = Literal["expenses", "excluded"]` e `_PREDICATE: dict[View, str]`; `_where` tem a assinatura `(view: View, date_from: str | None, date_to: str | None, account_id: str | None, search: str | None)`, chama `date_window(date_from, date_to, "t.date")` e não contém `" AND t.date >= ?"` literal; `sum_expenses`, `list_expenses` e `sum_by_category` têm o parâmetro `view: View = "expenses"`; `_SELECT` contém `t.not_expense_reason` e `_item` devolve a chave `"not_expense_reason"`; `get_expense` não chama `_where`. `app/taxonomy/override.py` define `Reason`, `REASONS`, `class NotAnOutflowError(ValueError)`, `def set_not_expense(conn: sqlite3.Connection, transaction_id: int, reason: Reason) -> None` e `def clear_not_expense(conn: sqlite3.Connection, transaction_id: int) -> None`; `set_not_expense` contém `{OUTFLOW}` e `SET not_expense_reason = ?`; `clear_not_expense` contém `SET not_expense_reason = NULL`; ambos chamam `_require(` e `_write(`. (estrutural)
- [x] CA1.5 — Migração: base anterior à `022` recebe a coluna nula que aceita os três valores e `NULL` e recusa `'x'`; base nova aplica 20 migrações. Predicado: linha marcada sai de `total_spending_cents`; `date_window` vazia e composta. Domínio: `set_not_expense` grava, reclassifica e persiste; `clear` volta a `NULL` e é idempotente; id desconhecido; transferência, estorno e entrada recusados sem gravar; `residue` deixa de contar linha marcada. Ingestão: linha nova entra nula; reingestão preserva o motivo e troca a descrição (os testes nomeados em T1.5: 1 novo em `tests/test_migrations.py` mais o renomeado, 3 em `tests/test_spending.py`, 6 em `tests/test_override.py`, 2 em `tests/test_ingest.py`); os testes anteriores desses arquivos continuam com o mesmo nome, exceto o renomeado. (comportamental)
- [x] CA1.6 — `uv run pytest --cov=app.queries.spending --cov=app.queries.expenses --cov=app.taxonomy.override --cov=app.taxonomy.classify --cov-report=term tests/test_spending.py tests/test_override.py tests/test_classify.py tests/test_expenses_api.py tests/test_ingest.py` reporta ≥ 80% em `app/queries/spending.py`, `app/queries/expenses.py`, `app/taxonomy/override.py` e `app/taxonomy/classify.py`. (comando)
- [x] CA1.7 — `docs/roadmap.md` tem a linha `| 025 | \`spending-filter-predicate-unification\` |` com status `done`; nenhuma outra linha do roadmap muda nesta fase. (estrutural)

## Fase 2 — `PUT`/`DELETE /api/transactions/{id}/not-expense`, `view` nos dois `GET`, os mocks MSW e `tests/test_expenses_api.py`

Python mais `src/testing/mocks/handlers.ts`, `src/features/expenses/types/expense.ts` (só o campo do item) e `src/features/expenses/components/__tests__/category-picker.test.tsx` (só o literal `BASE`). Ao final: a API marca e desmarca um lançamento, lista e agrupa por `view`, cada item carrega `not_expense_reason`, e o MSW responde o mesmo contrato para a fase 3.

- [x] T2.1 — Router: campo no `Expense`, `view` nos `GET`, `NotExpenseBody`, `PUT` e `DELETE`
  - Arquivos: `app/routers/transactions.py` (alterar)
  - O que fazer (api-requests; modelo: `update_category`):
    - Importa `View` de `app.queries.expenses` e `NotAnOutflowError`, `clear_not_expense`, `set_not_expense` de `app.taxonomy.override`. Constante `NOT_AN_OUTFLOW = "Só uma saída que conta como gasto pode ser marcada."` ao lado de `UNKNOWN_CATEGORY`.
    - `Expense` ganha `not_expense_reason: Literal["own_transfer", "refund", "other"] | None` (último campo). `class NotExpenseBody(BaseModel): reason: Literal["own_transfer", "refund", "other"]` (obrigatório, sem padrão), declarada depois de `ApplyToSimilarResponse`.
    - `expenses(...)` e `expenses_by_category(...)` ganham `view: Annotated[View, Query()] = "expenses"` (último parâmetro) e repassam `view=view` a `list_expenses` / `sum_by_category`. `month_signal_of_period` **não** ganha `view`.
    - `@router.put("/{transaction_id}/not-expense") def set_not_expense_of(transaction_id: int, body: NotExpenseBody) -> Expense` e `@router.delete("/{transaction_id}/not-expense") def clear_not_expense_of(transaction_id: int) -> Expense`, declaradas depois de `update_category` e antes de `similar`: `conn = connect()`; `try:` → `try: set_not_expense(conn, transaction_id, body.reason)` (ou `clear_not_expense(conn, transaction_id)`) `except UnknownTransactionError as error: raise HTTPException(status_code=404, detail=UNKNOWN_EXPENSE) from error` `except NotAnOutflowError as error: raise HTTPException(status_code=422, detail=NOT_AN_OUTFLOW) from error` (só no `PUT`); `item = get_expense(conn, transaction_id)`; `finally: conn.close()`; `item is None` → 404 `UNKNOWN_EXPENSE`; `return Expense(**item)`. Funções `def`, não `async def`; o arquivo continua sem `SELECT `, `INSERT `, `UPDATE `, `DELETE FROM` e `commit(`.
  - Skills: api-requests
  - Complexidade: baixa

- [x] T2.2 — Mocks MSW do motivo, da vista e das duas rotas
  - Arquivos: `src/testing/mocks/handlers.ts` (alterar); `src/features/expenses/types/expense.ts` (alterar); `src/features/expenses/components/__tests__/category-picker.test.tsx` (alterar)
  - O que fazer (api-mocking):
    - `expense.ts`: `export type NotExpenseReason = 'own_transfer' | 'refund' | 'other'` antes de `Expense`; `Expense` ganha `not_expense_reason: NotExpenseReason | null` (último campo). Nada mais nesta fase.
    - `category-picker.test.tsx`: o literal `BASE: Expense` ganha `not_expense_reason: null`. Nenhum teste muda de nome.
    - `handlers.ts`: importa `NotExpenseReason`; cada um dos seis literais de `generateFakeExpenses` ganha `not_expense_reason: null`. `filterExpenses(url)` lê `const view = url.searchParams.get('view')` e acrescenta ao `filter` a condição `(view === 'excluded' ? item.not_expense_reason !== null : item.not_expense_reason === null)`. Os handlers de `/api/transactions/expenses` e `/api/transactions/expenses/by-category` não mudam (herdam pela `filterExpenses`); o de `month-signal` também não. Dois handlers novos, depois de `http.patch('/api/transactions/:id/category', …)`: `http.put('/api/transactions/:id/not-expense', async ({ params, request }) => …)` — item não achado → 404 `{ detail: 'Gasto não encontrado.' }`; `body = (await request.json()) as { reason?: string }`; `reason` fora de `['own_transfer', 'refund', 'other']` ou ausente → 422 `{ detail: 'Motivo inválido.' }`; grava `item.not_expense_reason = body.reason as NotExpenseReason` e devolve `HttpResponse.json(item)`. `http.delete('/api/transactions/:id/not-expense', ({ params }) => …)` — 404 idem; `item.not_expense_reason = null`; devolve o item. `resetExpenses()` já recria os 45 itens nulos; nada em `setup.ts`.
  - Skills: api-mocking
  - Complexidade: baixa

- [x] T2.3 — Testes da fase 2
  - Arquivos: `tests/test_expenses_api.py` (alterar)
  - O que fazer (api-requests, unit-testing; reaproveita `client`, `_sign_in`, `_transaction`, `_load`, `_first_id`, `_by_category`, `_month_signal`, `MONTH`; helpers novos `_put_not_expense(client, id, body)` = `client.put(f"/api/transactions/{id}/not-expense", json=body)` e `_delete_not_expense(client, id)` = `client.delete(f"/api/transactions/{id}/not-expense")`; fixture de agosto `_load_not_expense_fixture()` = `_load([_transaction("ne-1", "2026-08-20", -60.0, categoria="Groceries", descricao="ACOUGUE"), _transaction("ne-2", "2026-08-15", -45.0, categoria="Healthcare", descricao="FARMACIA"), _transaction("ne-t", "2026-08-05", -500.0, eh_transferencia=True, descricao="TED")])`; `_id_by_description(client, text)` lendo `GET /expenses` — a transferência não aparece lá, então o seu id vem de `connect()` + `SELECT id FROM transactions WHERE pluggy_id = 'ne-t'`):
    - `test_the_response_has_the_contract_fields` — o conjunto de chaves ganha `"not_expense_reason"` (mesmo nome de teste).
    - `test_each_expense_carries_a_null_not_expense_reason` — fixture; todo item de `GET /expenses` tem `item["not_expense_reason"] is None`.
    - `test_put_not_expense_without_session_answers_401` — `_put_not_expense(client, 1, {"reason": "own_transfer"})` sem `_sign_in` → 401 `{"detail": "nao autenticado"}`; `_delete_not_expense(client, 1)` idem.
    - `test_put_not_expense_answers_the_expense_with_the_reason` — ACOUGUE com `{"reason": "own_transfer"}` → 200, `body["id"] == id`, `body["not_expense_reason"] == "own_transfer"`, `body["description"] == "ACOUGUE"`.
    - `test_a_marked_expense_leaves_the_list_the_totals_the_groups_and_the_month_signal` — antes: `GET /expenses?{MONTH}` tem `total == 2`, `total_cents == -10500`; `_by_category(MONTH)["total_cents"] == -10500` e há grupo "Supermercado"; `_month_signal(MONTH)["spent_cents"] == 10500`. Marca ACOUGUE; depois: `total == 1`, `total_cents == -4500`, descrições `== ["FARMACIA"]`; `by-category` sem "Supermercado" e `total_cents == -4500`; `month-signal` `spent_cents == 4500`.
    - `test_view_excluded_lists_only_the_marked_rows_with_their_reason_and_total` — marca ACOUGUE com `"refund"`; `GET /expenses?view=excluded&{MONTH}` → `total == 1`, `total_cents == -6000`, único item com `description == "ACOUGUE"` e `not_expense_reason == "refund"`; TED não aparece; `_by_category(f"view=excluded&{MONTH}")` → um grupo `"Supermercado"` com `total_cents == -6000`, `total_cents` do corpo `== -6000`.
    - `test_view_excluded_on_a_base_without_marks_is_empty` — fixture sem marcar; `GET /expenses?view=excluded` → `total == 0`, `items == []`; `by-category?view=excluded` → `groups == []`.
    - `test_an_unknown_view_answers_422` — `GET /expenses?view=x` e `GET /expenses/by-category?view=x` → 422.
    - `test_month_signal_does_not_accept_view` — `paths["/api/transactions/expenses/month-signal"]["get"]["parameters"]` do `/openapi.json` tem só os nomes `{"from", "to"}`.
    - `test_put_not_expense_refuses_a_reason_outside_the_list_and_a_missing_reason` — `{"reason": "x"}` e `{}` → 422 com `detail` lista (Pydantic); depois `GET /expenses` ainda lista ACOUGUE com `not_expense_reason is None`.
    - `test_put_and_delete_not_expense_on_an_unknown_expense_answer_404` — id `999999` → 404 `{"detail": "Gasto não encontrado."}` nos dois.
    - `test_put_not_expense_refuses_an_automatic_transfer_with_422` — id de `ne-t` → 422 `{"detail": "Só uma saída que conta como gasto pode ser marcada."}`; `GET /expenses?view=excluded` continua vazio.
    - `test_delete_not_expense_brings_the_row_back_and_is_idempotent` — marca ACOUGUE; `_delete_not_expense` → 200 com `not_expense_reason is None`; `GET /expenses?{MONTH}` volta a `total == 2` e `total_cents == -10500`; `view=excluded` vazio; segundo `DELETE` → 200 com `None`.
    - `test_a_not_expense_mark_survives_reingesting_the_same_source` — marca ACOUGUE com `"own_transfer"`; `_load` da mesma fixture; `GET /expenses?{MONTH}` tem `total == 1` sem ACOUGUE; `view=excluded` lista ACOUGUE com `"own_transfer"`.
    - `test_the_openapi_lists_put_and_delete_of_not_expense_and_view_on_both_gets` — `paths["/api/transactions/{transaction_id}/not-expense"]` tem `put` e `delete`; `"view"` está nos nomes de `parameters` de `paths["/api/transactions/expenses"]["get"]` e de `paths["/api/transactions/expenses/by-category"]["get"]`; `components.schemas.Expense.properties` tem `not_expense_reason`.
  - Skills: api-requests, unit-testing
  - Complexidade: média

### Critérios de aceite da fase 2

- [x] CA2.1 — `bash scripts/lint.sh` sai com código 0; `uv run pytest` passa; `bash scripts/gates/gates_runner.sh` sai com código 0; `pnpm lint`, `pnpm typecheck` e `pnpm test` saem com código 0. (comando)
- [x] CA2.2 — `app/routers/transactions.py` define `NOT_AN_OUTFLOW = "Só uma saída que conta como gasto pode ser marcada."`, `class NotExpenseBody(BaseModel)` com `reason: Literal["own_transfer", "refund", "other"]`, `Expense` com `not_expense_reason: Literal["own_transfer", "refund", "other"] | None`; tem `@router.put("/{transaction_id}/not-expense")` e `@router.delete("/{transaction_id}/not-expense")` sobre funções `def` (não `async def`) que devolvem `Expense`, ambas declaradas depois de `@router.patch("/{transaction_id}/category")` e antes de `@router.get("/{transaction_id}/similar")`; `expenses` e `expenses_by_category` têm o parâmetro `view: Annotated[View, Query()] = "expenses"` e `month_signal_of_period` não tem `view`; o arquivo contém `except NotAnOutflowError` com `status_code=422` e não contém `SELECT `, `INSERT `, `UPDATE `, `DELETE FROM` nem `commit(`. (estrutural)
- [x] CA2.3 — `src/features/expenses/types/expense.ts` exporta `NotExpenseReason = 'own_transfer' | 'refund' | 'other'` e `Expense` tem `not_expense_reason: NotExpenseReason | null`; `src/testing/mocks/handlers.ts` tem `not_expense_reason: null` em cada um dos seis literais de `generateFakeExpenses`, `filterExpenses` lê `searchParams.get('view')` e compara `not_expense_reason`, e há `http.put('/api/transactions/:id/not-expense'` (com 404 "Gasto não encontrado." e 422 "Motivo inválido.") e `http.delete('/api/transactions/:id/not-expense'` (com 404); `src/testing/setup.ts` não muda; `category-picker.test.tsx` tem `not_expense_reason: null` em `BASE` e os 16 `test(` anteriores mantêm o nome. (estrutural)
- [x] CA2.4 — API: item com o campo nulo; 401 sem sessão no `PUT` e no `DELETE`; `PUT` devolve o item marcado; a linha marcada sai da lista, dos totais, de `by-category` e de `month-signal`; `view=excluded` lista só a marcada com motivo e total nos dois `GET`; `view=excluded` vazio sem marcas; `view=x` → 422; `month-signal` sem `view`; `reason` inválido/ausente → 422 sem gravar; 404 nos dois; transferência automática → 422 com o texto; `DELETE` devolve e é idempotente; marcação sobrevive à reingestão; OpenAPI (os 14 testes novos e o alterado nomeados em T2.3); `tests/test_route_guard.py::test_every_registered_route_requires_session` passa; os testes anteriores de `tests/test_expenses_api.py` continuam com o mesmo nome. (comportamental)
- [x] CA2.5 — `uv run pytest --cov=app.routers.transactions --cov=app.queries.expenses --cov=app.taxonomy.override --cov-report=term tests/test_expenses_api.py tests/test_override.py` reporta ≥ 80% em `app/routers/transactions.py`, `app/queries/expenses.py` e `app/taxonomy/override.py`. (comando)

## Fase 3 — Tipos e operações de API, `ViewSelect`, `NotExpenseControl`, `ExpenseItem`, `Pagination`, `ExpensesList` com "Desfazer" e modo "Não são gastos", testes, e2e, `design.md` e roadmap

Ao final: em `/app/expenses`, cada linha tem "Não é gasto" → motivo → "Confirmar"; a linha some, os totais caem e o aviso verde oferece "Desfazer"; "Mostrar" alterna para "Não são gastos" (`?view=excluded`), onde a linha mostra o motivo em selo e "Voltar a ser gasto"; `docs/design.md` ganha "Aviso com desfazer"; `docs/roadmap.md` põe a 015 em `in-review`; a jornada é provada com API mockada e, contra o FastAPI real, pelo Playwright.

- [ ] T3.1 — Tipos da vista, rótulos do motivo e as quatro operações de API
  - Arquivos: `src/features/expenses/types/expense.ts` (alterar); `src/features/expenses/utils/reason-labels.ts` (criar); `src/features/expenses/api/get-expenses.ts` (alterar); `src/features/expenses/api/get-category-totals.ts` (alterar); `src/features/expenses/api/set-not-expense.ts` (criar); `src/features/expenses/api/clear-not-expense.ts` (criar)
  - O que fazer (api-requests; modelo de mutation: `update-category.ts`):
    - `expense.ts`: `export type ExpenseView = 'expenses' | 'excluded'`; `ExpensesQuery` ganha `view: ExpenseView`; `CategoryTotalsQuery = Pick<ExpensesQuery, 'from' | 'to' | 'account' | 'search' | 'view'>`.
    - `reason-labels.ts`: `export const REASON_OPTIONS: { value: NotExpenseReason; label: string }[]` na ordem `own_transfer` "Transferência entre minhas contas", `refund` "Estorno", `other` "Outro"; `export const REASON_LABELS: Record<NotExpenseReason, string>` com os mesmos textos.
    - `get-expenses.ts` e `get-category-totals.ts`: depois de `q`, `if (query.view === 'excluded') { params.set('view', 'excluded') }`. Chaves de query inalteradas (`['expenses', query]` e `['expenses', 'by-category', query]` já incluem `view` por estar no objeto).
    - `set-not-expense.ts`: `export interface SetNotExpenseVariables { id: number; reason: NotExpenseReason }`; `export function setNotExpense({ id, reason }: SetNotExpenseVariables): Promise<Expense>` = `apiRequest<Expense>(`/api/transactions/${String(id)}/not-expense`, { method: 'PUT', body: JSON.stringify({ reason }) })`; `export function useSetNotExpense()` = `useMutation({ mutationFn: setNotExpense, onSuccess })` com `void queryClient.invalidateQueries({ queryKey: ['expenses'] })`. Sem `setQueryData`, sem `onError`.
    - `clear-not-expense.ts`: `export function clearNotExpense(id: number): Promise<Expense>` = `apiRequest<Expense>(`/api/transactions/${String(id)}/not-expense`, { method: 'DELETE' })`; `export function useClearNotExpense()` idem, invalidando `['expenses']`.
  - Skills: api-requests
  - Complexidade: baixa

- [ ] T3.2 — `ViewSelect`, `NotExpenseControl` e a receita nova no `design.md`
  - Arquivos: `src/features/expenses/components/view-select.tsx` (criar); `src/features/expenses/components/not-expense-control.tsx` (criar); `docs/design.md` (alterar)
  - O que fazer (interface-design, client-state, error-handling, component-robustness; aparência pelo `docs/design.md` e pelo piso da skill; sem JSX copiado deste plano):
    - `docs/design.md`, uma linha nova na tabela "Padrões acrescentados pelas entregas", fatia `015`: **Aviso com desfazer** — bloco `role="status"` com `mt-6 flex flex-wrap items-center justify-between gap-3 rounded-md border border-green-200 bg-green-50 p-3`; texto `text-sm text-green-800`; à direita, botão secundário ("Desfazer"); em erro, o mesmo bloco com `role="alert"`, `border-red-200 bg-red-50` e texto `text-red-800`.
    - `view-select.tsx`: `export function ViewSelect({ value, onChange }: { value: ExpenseView; onChange: (view: ExpenseView) => void }): React.JSX.Element` — cópia da forma do `<select>` de `sort-controls.tsx` (controle em `flex flex-col gap-1`, `useId`, `<label>` ligado ao `<select>`, classes do `<select>` da "Barra de controles de lista"): rótulo "Mostrar"; opções na ordem `expenses` "Gastos" e `excluded` "Não são gastos"; `onChange` só com um dos dois valores. Sem botão.
    - `not-expense-control.tsx`: `export function NotExpenseControl({ expense, view, onExcluded }: { expense: Expense; view: ExpenseView; onExcluded: (excluded: { id: number; description: string | null }) => void }): React.JSX.Element` — molde de `similar-offer.tsx` (bloco de ação em linha, `isPending`, `isError`, "Tentar de novo" repetindo `mutation.variables`, retorno antecipado contra duplo clique). `const name = expense.description ?? 'Sem descrição'`.
      - Modo `expenses` (`view === 'expenses'`): `const [editing, setEditing] = useState(false)`, `const [reason, setReason] = useState<NotExpenseReason>('own_transfer')`, `const mutation = useSetNotExpense()`. Fechado: `Button variant="secondary"` "Não é gasto" com `aria-label={`Marcar ${name} como não-gasto`}` → `setEditing(true)`. Aberto: um contêiner que quebra em 360px (`mt-1 flex flex-wrap items-center justify-end gap-2`) com `<label>` visível "Motivo" (`text-sm`, ligado por `useId`) + `<select autoFocus>` (classes do `<select>` da barra de controles mais `text-sm`) com `REASON_OPTIONS` e `value={reason}`; `Escape` no `onKeyDown` do `<select>` → `setEditing(false)`; `Button variant="primary"` "Confirmar" (`disabled` e texto "Salvando…" enquanto `mutation.isPending`; `onClick` retorna cedo se pendente; `mutation.mutate({ id: expense.id, reason }, { onSuccess: () => { setEditing(false); onExcluded({ id: expense.id, description: expense.description }) } })`) e `Button variant="secondary"` "Cancelar" (`disabled` enquanto pendente) → `setEditing(false)`. Quando `mutation.isError`: acima dos botões, `<p role="alert">` "Não foi possível marcar como não-gasto." (`mt-1 text-sm text-red-700`) e o botão "Tentar de novo" (`Button variant="secondary"`) que chama `mutation.mutate(mutation.variables, { onSuccess: … })` com o mesmo `onSuccess`; o seletor fica aberto com o motivo escolhido.
      - Modo `excluded`: `const undo = useClearNotExpense()`; `Button variant="secondary"` "Voltar a ser gasto" com `aria-label={`Voltar ${name} a ser gasto`}` (`disabled` e "Salvando…" enquanto `undo.isPending`; retorno cedo se pendente; `undo.mutate(expense.id)`). `undo.isError` → `<p role="alert">` "Não foi possível voltar a contar como gasto." + "Tentar de novo" (`undo.mutate(expense.id)`).
      - Os dois hooks são chamados sempre (sem hook condicional); o que muda por `view` é o que se rende. Sem `useEffect`, sem `style={{`.
  - Skills: interface-design, client-state, error-handling, component-robustness
  - Complexidade: média

- [ ] T3.3 — `ExpenseItem` com selo do motivo e o controle, `Pagination` com `noun`, `ExpensesList` com `view`, aviso "Desfazer" e o vazio do modo `excluded`
  - Arquivos: `src/features/expenses/components/expense-item.tsx` (alterar); `src/features/expenses/components/pagination.tsx` (alterar); `src/features/expenses/components/expenses-list.tsx` (alterar)
  - O que fazer (interface-design, client-state, error-handling):
    - `expense-item.tsx`: props novas `view: ExpenseView` e `onExcluded: (excluded: { id: number; description: string | null }) => void`. Na coluna da direita, entre a data e o valor: em `expenses`, o `CategoryPicker` como hoje; em `excluded`, um `<span>` com as classes do "Selo de status" no par cinza mostrando `REASON_LABELS[expense.not_expense_reason ?? 'other']`. Abaixo do valor, `<NotExpenseControl expense={expense} view={view} onExcluded={onExcluded} />`. A coluna continua `flex shrink-0 flex-col items-end gap-1`.
    - `pagination.tsx`: prop opcional `noun?: { one: string; many: string }` com padrão `{ one: 'gasto', many: 'gastos' }`; `unit` passa a `total === 1 ? ` ${noun.one}` : ` ${noun.many}``. Nada mais muda.
    - `expenses-list.tsx`: `function readView(value: string | null): ExpenseView` (`'excluded'` → `'excluded'`; qualquer outro → `'expenses'`); `function writeView(params: URLSearchParams, view: ExpenseView): void` (`params.delete('page')`; `expenses` → `params.delete('view')`; `excluded` → `params.set('view', 'excluded')`). `const view = readView(searchParams.get('view'))`; `filters` ganha `view`; `useExpenses({ page, sort, order, ...filters })` já leva `view` por `filters`. `handleViewChange(next)` no molde de `handleAccountChange`. No `header`: `<ViewSelect value={view} onChange={handleViewChange} />` depois de `<SortControls …/>`, dentro da barra; `MonthCeiling` só com `period.kind === 'month' && view === 'expenses'`. Estado `const [lastExcluded, setLastExcluded] = useState<{ id: number; description: string | null } | null>(null)`; `const undo = useClearNotExpense()`; `useEffect(() => { setLastExcluded(null) }, [view])`. Elemento `notice`: quando `lastExcluded !== null`, o "Aviso com desfazer" (receita nova) com texto `${lastExcluded.description ?? 'Sem descrição'} não conta mais como gasto.` e `Button variant="secondary"` "Desfazer" (`disabled` e "Desfazendo…" enquanto `undo.isPending`; `undo.mutate(lastExcluded.id, { onSuccess: () => { setLastExcluded(null) } })`); quando `undo.isError`, o mesmo bloco em vermelho com `role="alert"`, texto "Não foi possível desfazer." e o botão "Tentar de novo" (mesma chamada). `notice` é rendido logo depois de `{header}` nos estados com dados **e** vazio (não em carregando/erro). Vazio: em `excluded`, "Nenhum lançamento marcado como não-gasto." (com ou sem filtro); em `expenses`, os textos de hoje. `ExpenseItem` recebe `view={view}` e `onExcluded={(excluded) => { undo.reset(); setLastExcluded(excluded) }}`. `Pagination` recebe `noun={view === 'excluded' ? { one: 'lançamento', many: 'lançamentos' } : undefined}`.
  - Skills: interface-design, client-state, error-handling
  - Complexidade: média

- [ ] T3.4 — e2e e roadmap
  - Arquivos: `e2e/expenses.spec.ts` (alterar); `docs/roadmap.md` (alterar)
  - O que fazer (e2e-testing; `playwright.config.ts` já tem `workers: 1`; o arquivo já está em `mode: 'serial'`):
    - Teste novo `marks an expense as not an expense, undoes it, lists it under "Não são gastos" and leaves the base as it found it`, depois de `shows the month against its ceiling, edits the ceiling inline and leaves the base as it found it` e antes de `goes back to the balances page`:
      1. Login como nos outros testes (`/app/login`, `Login`, `Senha`, "Entrar", `toHaveURL(/\/app\/?$/)`); `await page.goto('/app/expenses?month=2026-08')`; `await expect(page.getByText('Página 1 de 1 · 2 gastos · R$ 105,00 no período')).toBeVisible()`; `const row = page.getByRole('listitem').filter({ hasText: 'AÇOUGUE SÃO JORGE' })`.
      2. `await row.getByRole('button', { name: 'Marcar AÇOUGUE SÃO JORGE como não-gasto' }).click()`; `const reason = row.getByLabel('Motivo')`; `await expect(reason).toBeFocused()`; `await expect(reason).toHaveValue('own_transfer')`; `await row.getByRole('button', { name: 'Confirmar' }).click()`.
      3. `const notice = page.getByRole('status').filter({ hasText: 'AÇOUGUE SÃO JORGE não conta mais como gasto.' })`; `await expect(notice).toBeVisible()`; `await expect(page.getByRole('listitem')).toHaveCount(1)`; `await expect(page.getByText('Página 1 de 1 · 1 gasto · R$ 45,00 no período')).toBeVisible()`; `await expect(page.getByRole('table', { name: 'Por categoria' })).not.toContainText('Supermercado')`.
      4. `await notice.getByRole('button', { name: 'Desfazer' }).click()`; `await expect(notice).toHaveCount(0)`; `await expect(page.getByText('Página 1 de 1 · 2 gastos · R$ 105,00 no período')).toBeVisible()`; `await expect(row).toBeVisible()`.
      5. Marca de novo (passo 2, mesmo motivo); `await expect(page.getByText('Página 1 de 1 · 1 gasto · R$ 45,00 no período')).toBeVisible()`.
      6. `await page.getByLabel('Mostrar').selectOption('excluded')`; `await expect(page).toHaveURL(/view=excluded/)`; `await expect(page.getByRole('listitem')).toHaveCount(1)`; `await expect(page.getByRole('listitem')).toContainText('AÇOUGUE SÃO JORGE')`; `await expect(page.getByRole('listitem').getByText('Transferência entre minhas contas', { exact: true })).toBeVisible()`; `await expect(page.getByText('Página 1 de 1 · 1 lançamento · R$ 60,00 no período')).toBeVisible()`; `await expect(page.getByRole('heading', { level: 2, name: 'Teto do mês' })).toHaveCount(0)`.
      7. `await page.reload()`; `await expect(page.getByLabel('Mostrar')).toHaveValue('excluded')`; `await expect(page.getByRole('listitem')).toHaveCount(1)`.
      8. `await page.getByRole('button', { name: 'Voltar AÇOUGUE SÃO JORGE a ser gasto' }).click()`; `await expect(page.getByText('Nenhum lançamento marcado como não-gasto.')).toBeVisible()`.
      9. `await page.getByLabel('Mostrar').selectOption('expenses')`; `await expect(page).not.toHaveURL(/view=/)`; `await expect(page.getByText('Página 1 de 1 · 2 gastos · R$ 105,00 no período')).toBeVisible()`.
      Os nove `test(` existentes do arquivo, `e2e/categories.spec.ts`, `e2e/apply-to-similar.spec.ts`, `e2e/login-and-balances.spec.ts`, `e2e/sync.spec.ts`, `scripts/e2e-backend.sh` e `playwright.config.ts` não mudam.
    - `docs/roadmap.md`: na linha `| 015 | \`marcar-nao-gasto\` |`, o status `in-progress` passa a `in-review`. Nenhuma outra linha muda nesta fase.
  - Skills: e2e-testing
  - Complexidade: baixa

- [ ] T3.5 — Testes da fase 3
  - Arquivos: `src/features/expenses/components/__tests__/view-select.test.tsx` (criar); `src/features/expenses/components/__tests__/not-expense-control.test.tsx` (criar); `src/features/expenses/components/__tests__/expenses-list.test.tsx` (alterar); `src/features/expenses/components/__tests__/pagination.test.tsx` (alterar)
  - O que fazer (component-testing, api-mocking; `renderWithProviders` de `@/testing/test-utils` — devolve `queryClient`; `server` de `@/testing/mocks/server`; `fakeExpenses` de `@/testing/mocks/handlers`; temporizador só com `fireEvent` + `vi.waitFor`, nunca `vi.useFakeTimers` em arquivo novo):
    - `view-select.test.tsx`:
      - `shows the label "Mostrar" with the options in order` — `getByLabelText('Mostrar')` é `combobox`; opções `['Gastos', 'Não são gastos']` com valores `['expenses', 'excluded']`; `value="expenses"` selecionado.
      - `calls onChange with the chosen view` — `selectOptions('excluded')` → `onChange` chamado uma vez com `'excluded'`.
    - `not-expense-control.test.tsx` (helper `BASE: Expense` como o de `category-picker.test.tsx`, `id: 44`, `description: 'GASTO 44'`, `not_expense_reason: null`; helpers `spyOnPut()` e `spyOnPendingPut()` no molde de `spyOnApply`/`spyOnPendingApply` de `similar-offer.test.tsx`, sobre `http.put('/api/transactions/:id/not-expense')` devolvendo `{ ...BASE, not_expense_reason: body.reason }`; `spyOnDelete()` sobre `http.delete`; `renderControl(view, onExcluded = vi.fn())`):
      - `shows "Não é gasto" and no combobox when closed` — botão com nome acessível "Marcar GASTO 44 como não-gasto" e texto "Não é gasto"; `queryByRole('combobox')` nulo.
      - `uses "Sem descrição" in the accessible name when the description is null`.
      - `opens the reason select with focus and "Transferência entre minhas contas" selected` — clique → `combobox` "Motivo" com foco, `value === 'own_transfer'`, opções na ordem "Transferência entre minhas contas", "Estorno", "Outro"; botões "Confirmar" e "Cancelar"; o botão "Não é gasto" some.
      - `"Cancelar" closes without calling the API` — `calls.length === 0`; botão "Não é gasto" de volta.
      - `Escape closes without calling the API`.
      - `"Confirmar" with "Estorno" sends PUT with reason refund, shows "Salvando…" and calls onExcluded` — `spyOnPendingPut`; `selectOptions('refund')`; "Confirmar" → texto "Salvando…", "Confirmar" e "Cancelar" `disabled`; `release()`; `waitFor` `calls` `[{ id: '44', body: { reason: 'refund' } }]` e `onExcluded` chamado uma vez com `{ id: 44, description: 'GASTO 44' }`; o seletor fecha.
      - `clicking "Confirmar" twice while pending sends one call`.
      - `a 500 shows the alert and "Tentar de novo" repeats the same call` — primeira resposta 500, segunda 200: `role="alert"` "Não foi possível marcar como não-gasto."; `combobox` ainda aberto com `'refund'`; "Tentar de novo" → segunda chamada com `{ reason: 'refund' }` e `onExcluded` chamado.
      - `saving invalidates the expenses queries` — `vi.spyOn(queryClient, 'invalidateQueries')`; após confirmar, `waitFor` de `toHaveBeenCalledWith(expect.objectContaining({ queryKey: ['expenses'] }))`.
      - `in the excluded view shows "Voltar a ser gasto" that sends DELETE` — `view="excluded"`, `BASE` com `not_expense_reason: 'refund'`; botão "Voltar GASTO 44 a ser gasto"; clique → `DELETE` com `id '44'`; "Salvando…" enquanto pendente; `invalidateQueries` com `['expenses']`.
      - `in the excluded view a 500 shows the alert and "Tentar de novo" repeats` — "Não foi possível voltar a contar como gasto."; segunda chamada.
    - `pagination.test.tsx`:
      - `uses the noun given for the singular and the plural` — `noun={{ one: 'lançamento', many: 'lançamentos' }}` com `total={1}` → "1 lançamento"; `total={2}` → "2 lançamentos". Os 7 testes anteriores mantêm o nome.
    - `expenses-list.test.tsx` (mock padrão de `handlers.ts`; item 45 é "MERCADO DO BAIRRO", −8490, `2026-08-01`, "Compras"; helpers `categoryRows`/`cellsOf` existentes):
      - `marking a row as not an expense removes it, drops the totals and shows the undo notice` — `route: '/expenses'`; na primeira linha, clique em "Marcar MERCADO DO BAIRRO como não-gasto", "Confirmar"; `findByRole('status')` com texto "MERCADO DO BAIRRO não conta mais como gasto." e botão "Desfazer"; `waitFor`: nenhuma linha com "MERCADO DO BAIRRO"; a linha "Compras" da tabela cai (o total muda em `R$ 84,90`); o rodapé mostra "44 gastos".
      - `"Desfazer" brings the row back and removes the notice` — depois do caso acima, clique em "Desfazer" → `waitFor` linha "MERCADO DO BAIRRO" de volta, `queryByRole('status')` sem o texto, rodapé "45 gastos".
      - `a failed undo turns the notice into an alert with "Tentar de novo"` — `server.use` de `DELETE` com 500 na primeira chamada e 200 na segunda; "Desfazer" → `role="alert"` "Não foi possível desfazer." com "Tentar de novo"; clique → aviso some e a linha volta.
      - `marking the last row of a filter shows the notice above the empty state` — `route: '/expenses?month=2026-08'` (só o item 45); marca; `findByRole('status')` com o aviso e "Nenhum gasto para esse filtro." visíveis ao mesmo tempo.
      - `?view=excluded with no marks shows the excluded empty state and no month ceiling` — `route: '/expenses?view=excluded&month=2026-08'`; "Nenhum lançamento marcado como não-gasto."; `queryByText('Teto do mês')` nulo; "Mostrar" com `value 'excluded'`.
      - `?view=excluded lists the marked rows with the reason badge and "Voltar a ser gasto" returns them` — `fakeExpenses[0].not_expense_reason = 'own_transfer'` antes de renderizar; `route: '/expenses?view=excluded'`; uma linha "MERCADO DO BAIRRO" com o texto "Transferência entre minhas contas" e sem botão `/^Trocar categoria de /`; rodapé "1 lançamento · R$ 84,90 no período"; clique em "Voltar MERCADO DO BAIRRO a ser gasto" → `waitFor` "Nenhum lançamento marcado como não-gasto.".
      - `an unknown view falls back to the expenses view` — `route: '/expenses?view=x'`; "Mostrar" com `value 'expenses'`; 20 linhas; a chamada a `/api/transactions/expenses` (via `spyOnExpensesRequests`) não tem `view`.
      - `changing "Mostrar" writes view=excluded, drops the page and sends view to the API` — `route: '/expenses?page=2'` com `LocationProbe` e `spyOnExpensesRequests`; `selectOptions('excluded')` → `search` contém `view=excluded` e não contém `page=`; a última chamada tem `view === 'excluded'`; voltar a "Gastos" remove `view` da URL.
      - `changing the view clears the undo notice` — marca o item 45 em `'/expenses'`; troca "Mostrar" para `excluded` → `queryByRole('status')` sem "não conta mais como gasto".
  - Skills: component-testing, api-mocking
  - Complexidade: média

### Critérios de aceite da fase 3

- [ ] CA3.1 — `pnpm lint`, `pnpm typecheck`, `pnpm test` e `pnpm build` saem com código 0; `bash scripts/lint.sh`, `uv run pytest` e `bash scripts/gates/gates_runner.sh` também. (comando)
- [ ] CA3.2 — `pnpm test:e2e` sai com código 0; `e2e/expenses.spec.ts` tem um `test(` de nome `marks an expense as not an expense, undoes it, lists it under "Não são gastos" and leaves the base as it found it` contendo `'/app/expenses?month=2026-08'`, `'Marcar AÇOUGUE SÃO JORGE como não-gasto'`, `'Motivo'`, `'Confirmar'`, `'AÇOUGUE SÃO JORGE não conta mais como gasto.'`, `'Página 1 de 1 · 1 gasto · R$ 45,00 no período'`, `'Desfazer'`, `'Página 1 de 1 · 2 gastos · R$ 105,00 no período'`, `selectOption('excluded')`, `/view=excluded/`, `'Transferência entre minhas contas'`, `'Página 1 de 1 · 1 lançamento · R$ 60,00 no período'`, `page.reload()`, `'Voltar AÇOUGUE SÃO JORGE a ser gasto'`, `'Nenhum lançamento marcado como não-gasto.'` e `selectOption('expenses')`; os nove `test(` anteriores do arquivo continuam com o mesmo nome; `playwright.config.ts` contém `workers: 1`; `e2e/categories.spec.ts`, `e2e/apply-to-similar.spec.ts`, `e2e/login-and-balances.spec.ts`, `e2e/sync.spec.ts`, `scripts/e2e-backend.sh` e `playwright.config.ts` não mudam. (comando, estrutural)
- [ ] CA3.3 — `src/features/expenses/types/expense.ts` exporta `ExpenseView = 'expenses' | 'excluded'`, `ExpensesQuery` tem `view: ExpenseView` e `CategoryTotalsQuery` inclui `'view'` no `Pick`; `src/features/expenses/utils/reason-labels.ts` exporta `REASON_OPTIONS` (três entradas, nesta ordem de `value`: `own_transfer`, `refund`, `other`) e `REASON_LABELS` com os textos "Transferência entre minhas contas", "Estorno" e "Outro"; `get-expenses.ts` e `get-category-totals.ts` contêm `params.set('view', 'excluded')` guardado por `query.view === 'excluded'`; `set-not-expense.ts` exporta `setNotExpense` (com `method: 'PUT'`, `/not-expense` e `JSON.stringify({ reason })`) e `useSetNotExpense`; `clear-not-expense.ts` exporta `clearNotExpense` (com `method: 'DELETE'` e `/not-expense`) e `useClearNotExpense`; os dois hooks contêm `invalidateQueries({ queryKey: ['expenses'] })` e nenhum contém `setQueryData` nem `onError`. (estrutural)
- [ ] CA3.4 — `view-select.tsx` exporta `ViewSelect({ value, onChange }: { value: ExpenseView; onChange: (view: ExpenseView) => void })`, contém "Mostrar", "Gastos" e "Não são gastos", usa `useId` e um `<label htmlFor`. `not-expense-control.tsx` exporta `NotExpenseControl({ expense, view, onExcluded }: …)`, contém "Não é gasto", "Motivo", "Confirmar", "Cancelar", "Salvando…", "Tentar de novo", "Não foi possível marcar como não-gasto.", "Voltar a ser gasto", "Não foi possível voltar a contar como gasto.", `como não-gasto`` (no `aria-label` de marcar), `a ser gasto`` (no `aria-label` de voltar), `autoFocus`, `'Escape'`, `REASON_OPTIONS`, `useSetNotExpense(`, `useClearNotExpense(`, `mutation.variables`, `role="alert"`, e não contém `useEffect` nem `style={{`. `expense-item.tsx` tem as props `view` e `onExcluded`, importa `NotExpenseControl` e `REASON_LABELS`, e rende `<CategoryPicker` só quando `view` não é `'excluded'`. `pagination.tsx` tem a prop opcional `noun` com padrão `gasto`/`gastos`. `expenses-list.tsx` define `readView` e `writeView` (esta com `params.delete('page')`), importa `ViewSelect` e `useClearNotExpense`, contém `period.kind === 'month' && view === 'expenses'`, `lastExcluded`, "não conta mais como gasto.", "Desfazer", "Desfazendo…", "Não foi possível desfazer.", "Nenhum lançamento marcado como não-gasto.", `'lançamento'` e `'lançamentos'`; `<ViewSelect` aparece depois de `<SortControls` e antes do fechamento da barra de controles. (estrutural)
- [ ] CA3.5 — Piso visual, lido no código: `docs/design.md` tem a linha "Aviso com desfazer" na tabela "Padrões acrescentados pelas entregas" com fatia `015`; em `expenses-list.tsx` o aviso usa `rounded-md border border-green-200 bg-green-50 p-3` com texto `text-sm text-green-800` e tem `role="status"`, e a variante de erro usa `border-red-200 bg-red-50`, `text-red-800` e `role="alert"`; o botão "Desfazer" é `Button` com `variant="secondary"`; em `view-select.tsx` o `<select>` usa as classes do `<select>` da "Barra de controles de lista" (`min-h-10 rounded-md border border-gray-300 px-3 py-2 text-gray-900 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600`) e o rótulo `block text-sm font-medium text-gray-900`; em `not-expense-control.tsx` o contêiner do seletor tem `flex flex-wrap` e `gap-2`, o `<select>` tem `min-h-10` e `text-sm`, o erro é `<p role="alert">` com `text-sm text-red-700`, "Confirmar" é `Button` `variant="primary"` e "Cancelar"/"Não é gasto"/"Voltar a ser gasto"/"Tentar de novo" são `Button` `variant="secondary"`; em `expense-item.tsx` o selo do motivo usa `rounded-full px-2 py-0.5 text-sm bg-gray-100 text-gray-700`; nenhum dos cinco arquivos contém `style={{`, `<a href` ou `!important`. (estrutural)
- [ ] CA3.6 — Seletor: rótulo e opções em ordem, `onChange` (os 2 testes de `view-select.test.tsx`). Controle: fechado, "Sem descrição", abrir com foco e padrão, cancelar, `Escape`, confirmar com `refund` e `onExcluded`, duplo clique, 500 com "Tentar de novo", invalidação, modo `excluded` com `DELETE` e o erro dele (os 11 de `not-expense-control.test.tsx`). Paginação: `noun` (o 1 novo de `pagination.test.tsx`). Página: marcar tira a linha e os totais e mostra o aviso; desfazer devolve; desfazer com erro vira alerta; último da página com vazio; `view=excluded` vazio sem teto; `view=excluded` com selo e "Voltar a ser gasto"; `view` desconhecido; trocar "Mostrar" grava a URL e a API; trocar a vista limpa o aviso (os 9 de `expenses-list.test.tsx`). (comportamental)
- [ ] CA3.7 — Os 2 testes de `view-select.test.tsx`, os 11 de `not-expense-control.test.tsx`, o 1 novo de `pagination.test.tsx` e os 9 novos de `expenses-list.test.tsx` existem com os nomes de T3.5; os testes anteriores de `expenses-list.test.tsx`, `pagination.test.tsx`, `category-picker.test.tsx`, `similar-offer.test.tsx`, `category-totals.test.tsx` e `month-ceiling.test.tsx` continuam com o mesmo nome; `npx vitest run --coverage` reporta ≥ 80% de linhas em `src/features/expenses/components/view-select.tsx`, `src/features/expenses/components/not-expense-control.tsx`, `src/features/expenses/components/expense-item.tsx`, `src/features/expenses/components/pagination.tsx`, `src/features/expenses/components/expenses-list.tsx`, `src/features/expenses/api/set-not-expense.ts`, `src/features/expenses/api/clear-not-expense.ts` e `src/features/expenses/utils/reason-labels.ts`. (comando)
- [ ] CA3.8 — `docs/roadmap.md` tem a linha `| 015 | \`marcar-nao-gasto\` |` com status `in-review` e a linha `| 025 | \`spending-filter-predicate-unification\` |` com status `done`; nenhuma outra linha do roadmap muda. (estrutural)

## DoD da entrega

- [ ] DoD1 — Todas as tarefas e critérios do plano marcados
- [ ] DoD2 — Suíte de testes inteira passa
- [ ] DoD3 — Lint do projeto inteiro sem erros nem avisos
- [ ] DoD4 — Tipos de todos os `tsconfig` sem erros
- [ ] DoD5 — Console dos testes sem erro nem aviso
- [ ] DoD6 — `build` passa
- [ ] DoD7 — Nenhum import entre features nem contra o fluxo compartilhado → features → app
- [ ] DoD8 — Nenhum `console.log`, `TODO`, `// @debug`, `.only(` ou `.skip(` no diff da branch
- [ ] DoD9 — Nenhuma worktree ou branch temporária sobrando
